# 性能诊断报告

**扫描范围**: peri-tui（kit/、acp_events/、message_area/、status_bar/）+ peri-acp（event/、session/）+ peri-agent（agent/ 事件链路）
**扫描时间**: 2026-08-02
**项目类型**: Rust TUI（ratatui-kit 组件框架 + atom 全局状态）→ ACP 会话 → agent 循环
**用户报告场景**: 同时运行 3 个同步 agent 时 CPU 占用 50%

## 统计

- 高风险（已验证）: 0
- 中风险（已验证）: 7
- 低置信度: 5
- 已排除: 3
- 方法: A 路（调用链遍历）+ B 路（气味扫描）并行探索 → 8 路 explorer 对抗验证（4 角检查）

---

## 核心机制链（3 agent → 50% CPU 的成因）

验证确认：没有单一"失控级"缺陷，而是**每 token 全量重建 + 固定 20Hz 心跳 + per-token 日志**三条路径在 3 路并发 token 流下线性叠加：

```
每 token 到达（主/子 agent 文本、推理 chunk）
  → acp_bridge 逐事件同步 dispatch，无批处理/无节流
  → push_view_models：克隆 current_turn 全部 VM（O(N) 深拷贝）+ 写 VIEW_MODELS atom
  → 渲染一帧：message_area O(items) hash 扫描 + concat_wrap_maps O(总行数) 重建
             + 流式 bubble 全量 markdown 重解析 O(N)
  + loading 期固定 20Hz RENDER_HEARTBEAT → 无 token 时也整树重绘
  + 每 token 2 条 tracing::info! 同步写文件
3 个 agent = 3 路 token 流 × 上述成本，且全局 is_loading 几乎连续 → 20Hz 下限 + token 率上限
```

---

## 中风险（已验证）

### 1. Loading 期 20Hz 全树重绘心跳 — entry.rs:204-220

- **位置**: `peri-tui/src/kit/entry.rs:204-220`（+ `app_shell.rs:29` 订阅、ratatui-kit tree.rs render_loop）
- **模式**: TUI 渲染（渲染风暴）
- **描述**: is_loading 期间每 50ms 无条件 `RENDER_HEARTBEAT.set()`，AppShell 订阅该 atom → 整树 update+draw 20 次/秒。3 agent 并发时 loading 几乎连续，构成"无事件也高频重绘"的固定 20Hz 下限；且渲染循环无 dirty-check/合帧，任何 atom 写立即整帧渲染。
- **验证结论**:
  - 调用频率: 通过 — 50ms tick 无条件 set（entry.rs:210-215），reactive_handle.rs Drop 无条件 wake，tree.rs 每轮无条件 render
  - 替代路径: 通过 — 无 props 相等检查、无备忘录跳过、无合帧
  - 实际影响: 部分通过 — 机制真实但为线性常数成本（非反馈回路），ratatui buffer diff 吸收终端 I/O；20Hz 低于项目已处理过的 60-125Hz spike
  - 正确性必要: 通过 — 心跳本体是防 render loop 阻塞的刻意设计；代价在全树重渲染
- **最终**: MEDIUM

### 2. 每 token 全量 VM 重建与深拷贝 — acp_types.rs:646-679, 400-535

- **位置**: `peri-tui/src/kit/acp_types.rs:646-679`（SubAgentAccumulator::view_model）、`:400-535`（build_view_models）、`peri-tui/src/kit/acp_events/render.rs:23-26`（push_view_models）
- **模式**: 分配浪费（每事件全量重建）
- **描述**: 流式期间每个文本 chunk 使缓存失效（append_text/append_reasoning 全部 `replace(None)`），下次 push 时：clone 整个 child_turn（含 raw_input JSON 与 output_summary 深拷贝）→ 从 0 重建全部 child VM → 所有 child hash 拼 String 再 format → 再深拷贝进 im::Vector；父层 build_view_models 对每个 segment 全量重 hash（reasoning 非空时 format 拼接整段文本）。单 agent 每 token O(N)，累积 O(N²)/轮；3 并发放大 2-3 倍（非数量级——未写入的 subagent 命中缓存）。
- **验证结论**:
  - 调用频率: 通过 — 缓存粒度 = 1 token，每 token 必未命中（acp_types.rs:127,146,164,625,630）
  - 替代路径: 通过 — 无增量 hash、无事件批处理、无 debounce（acp_bridge.rs:105 逐事件消费）
  - 实际影响: 部分通过 — 单 token 数十~数百 µs；MessageArea 有按 content_hash 分片缓存缓解 markdown 重解析，但 O(N) 遍历与深拷贝每 token 必发生
  - 正确性必要: 通过 — content_hash 必须随内容变化（M1 需求）；但全量重建与深拷贝是实现选择
- **最终**: MEDIUM

### 3. 流式 bubble 每 token 全量 markdown 重解析 — markdown/mod.rs:137-138

- **位置**: `peri-tui/src/kit/markdown/mod.rs:137-138, 176-203`
- **模式**: 每事件全量重建（解析）
- **描述**: parse_markdown_cached 名义增量（convert 层跳过已闭合 block），但每次仍对全量累积文本执行 `ensure_closed_code_fences`（O(N) 逐行扫描 + 复制）和 `rk_parse`（pulldown-cmark 从零全量解析），文本以 \n 结尾时整串 clone 持久化；命中路径仍深拷贝已累积 Vec<Line> + 输出再拷贝。单 bubble O(N)/token，长消息累积 O(N²)。设计文档承诺"流式单次成本 O(W)"未兑现（docs/design/peri-tui-message-pipeline-v2.md:12）。
- **验证结论**:
  - 调用频率: 通过 — 流式 bubble 每 token hash 变化 → 每 token 触发一次解析
  - 替代路径: 通过 — 主路径仅 vm_to_lines_cached 一条路；syntect 高亮已有 LRU 缓存（最贵部分已规避）
  - 实际影响: 部分通过 — pulldown-cmark 解析 4-8KB 约 20-60µs，占帧预算 <0.5%；3 并发约 1-3% CPU，未达"CPU 拉满"级
  - 正确性必要: 通过 — pulldown-cmark 无增量 API，全量重解析是保证 fence 奇偶/表格翻转正确性的最简路径
- **最终**: MEDIUM

### 4. concat_wrap_maps 每帧线性重建 — message_area/mod.rs:289-307

- **位置**: `peri-tui/src/kit/message_area/mod.rs:289-307`（+ `selection.rs:52-70`）
- **模式**: 每帧全量重算
- **描述**: 每次渲染无条件遍历全部 slot 重建扁平 wrap_map：对每个逻辑行条目（32B）全量拷贝 + 偏移运算 + Vec 分配，非浅拷贝。消息区内唯一随会话长度线性增长的每帧成本，**内容未变时也执行**；长会话 3000 逻辑行 ≈ 96KB 拷贝 ≈ 十几~几十 µs/帧，20Hz 心跳 + 拖拽 60-120Hz 下放大。
- **验证结论**:
  - 调用频率: 通过 — 每次渲染无条件执行，无跳过（内容无变化时仍执行）
  - 替代路径: 通过 — 扁平 wrap_map 对视口二分/选区必需，但 slot hash 与 width 未变时结果必然相同，本可缓存
  - 实际影响: 通过 — 真实线性每帧成本，随会话增长
  - 正确性必要: 部分通过 — 机制必要，每帧重建不必要
- **最终**: MEDIUM

### 5. per-token 同步文件日志 — streaming.rs:70-73, render.rs:14-22

- **位置**: `peri-tui/src/kit/acp_events/streaming.rs:70-73`、`peri-tui/src/kit/acp_events/render.rs:14-22`
- **模式**: 日志开销（热路径 I/O）
- **描述**: 每个主 agent reasoning chunk 打一条 info!（streaming.rs，不受 streaming_mode 门控）+ 每个 text/reasoning chunk 打一条 `tracing::info!(target:"msg_scroll_diag")`（render.rs，默认 Streaming 模式每 token 一次，主/子 agent 全计）。默认 filter 为 info 且写入 RollingFileAppender（同步阻塞写文件，无用户态缓冲）。100 token/s × 3 agent ≈ 数百行/s。三项均标注为"临时 instrumentation/诊断"。
- **验证结论**:
  - 调用频率: 部分通过 — (a)(b) 每 token 属实；(c) acp_bridge [CLEAR_DEBUG] 仅 VM 计数变化时触发，非严格每 token
  - 替代路径: 通过 — 默认配置下全部生效，仅 RUST_LOG=warn 或 streaming_mode=none 可规避
  - 实际影响: 通过 — 真实但相对 push_view_models 自身热路径为小头
  - 正确性必要: 不通过 — 均为诊断/临时 instrumentation，无一是正确性必需
- **最终**: MEDIUM

### 6. 周期任务粗糙实现 — workflow/service/acp_bridge/status_bar

- **位置**: `peri-tui/src/kit/workflow_snapshot.rs:84-109`、`service_snapshot.rs:74,191,210,269,276-277`、`acp_bridge.rs:94-103`、`status_bar.rs:37,68-92,158-185`
- **模式**: 轮询 / 分配浪费 / 无 memo
- **描述**: 4 项独立低效：① workflow 每 2s 无条件写 WORKFLOW_SNAPSHOT（无 write_if_changed）+ 每次 list_runs RPC + 全量 serde 解析（顺带驱动壁钟运行动画，加守卫动画会停）；② service 每 2s 无条件 clone slow.threads/files（30s 才刷新）+ clone 静态 hooks/plugins 列表（有 write_if_changed 守卫，空闲 CPU 稳定时被抑制）；③ bash 运行期每 1s 无条件 invalidate_cache + 全量写 VIEW_MODELS；④ status_bar 渲染路径无 memo：整份快照 clone + 多个 to_string + 克隆全部 spans 量宽。
- **验证结论**:
  - 调用频率: 通过 — 各周期与守卫逐行核实
  - 替代路径: 部分通过 — atom 写是细粒度唤醒（WakerMap），非全树；面板关闭时 workflow 写零订阅者；bash 运行期 = loading 期，边际帧 ≈ +5%
  - 实际影响: 部分通过 — 单项均 LOW，但 workflow 恒定 RPC 与静态数据重复克隆是真实残留成本
  - 正确性必要: 通过 — 周期刷新本身是功能需求（运行动画、计时），只是实现粗糙
- **最终**: MEDIUM

### 7. event_sink per-token 序列化分配 — event_sink.rs:138-151

- **位置**: `peri-acp/src/session/event_sink.rs:138-151`
- **模式**: Serde/序列化（热路径分配 churn）
- **描述**: 每个 chunk 事件（TextChunk ≈ 每 token）serde_json::to_value(SessionUpdate) + json! 包层 + mapper chunk.clone()。进程内 mpsc 无字节序列化，纯分配 churn（每 token ≥2 次 Value 树构造，subagent 另有 _peri 第 3 次），无 I/O、无 backpressure。
- **验证结论**:
  - 调用频率: 通过 — TextChunk 确为逐 SSE delta（≈每 token），render 事件唯一活跃路径
  - 替代路径: 通过 — v2_bridge 有直连映射但 forwarder.rs:65-69 明确 render 事件不走 v2_channel（防双送）
  - 实际影响: 通过 — 真实但量级中等（纯分配，无 I/O）
  - 正确性必要: 通过 — 序列化是交付必需，_peri 是 subagent 路由必需
- **最终**: MEDIUM

---

## 低置信度

> 以下发现在验证阶段被部分驳回，但仍保留供参考。

- **子 agent chunk 绕过 Block 边界检测** — `peri-tui/src/kit/acp_events/streaming.rs:18-20,63-65`：mode≠None 时每 chunk 无条件 push_view_models。默认 Streaming 下主 agent 本就每 token push，独有影响仅在 Block 模式（主文本被边界节流、子 agent 不节流）。注释自认"subagent 输出相对短"。→ LOW（当前默认配置下不放大）
- **acp_notifier 每 chunk 多次 to_string** — `peri-tui/src/kit/acp_notifier.rs:381-407`：实测主 agent 路径仅 1 次分配（text），message_id/agent_id 恒为 None；仅 subagent 事件多 1 次。→ LOW
- **Langfuse 启用时 render 事件 ev.clone()** — `peri-acp/src/event/forwarder.rs:71-75`：默认关闭（需双环境变量），仅 opt-in 遥测开启时才有 per-token 开销。→ LOW（条件性）
- **v2_bridge 每 ReAct 迭代递增 RENDER_HEARTBEAT** — `peri-tui/src/kit/v2_bridge.rs:161-170`：原定位 acp_notifier.rs:180-182 是死代码（StateSnapshotMeta 无生产构造点）；真实点每迭代一次，一轮仅数次，被 20Hz tick 淹没。→ LOW
- **每帧 item_hashes 全量收集比对** — `message_area/mod.rs:194-224`：机制真实但 content_hash() 是字段读取 O(1)/item，1000 items 仅 1-3µs/帧，常数极小。→ LOW

---

## 已排除

- **render_subagent_group_lines 无缓存全量 markdown 解析**（A 路候选）：验证发现 `render.rs:787-789` 无条件跳过 TuiAssistantBubble 子 VM，group 内仅渲染 ToolCard/SystemNote（纯 span 拼装，无 markdown 解析）→ 核心影响被推翻
- **acp_bridge [CLEAR_DEBUG] 每 token 日志**：门控为 VM 计数变化（bubble 数），非 token 数，首个 chunk/新 message/subagent 启停才触发
- **锁/阻塞类**：全扫描未发现 std Mutex/RwLock 跨 .await、block_on、忙轮询；run_react_loop 为事件驱动（await_wake），无忙等

---

## 结论

3 agent 并发 50% CPU 无单一"元凶"，而是每 token 全量重建（VM 深拷贝 + hash + markdown 重解析 + wrap_map 重建）、loading 期 20Hz 整树重绘、per-token 同步文件日志三条路径叠加 3 路 token 流的结果。全部 7 项中风险均为"浪费型"而非"失控型"，无正确性缺陷。修复优先级建议方向（本轮未实施）：降低渲染频率下限（心跳节流/合帧）、消除 per-token 深拷贝与全量重算（增量 hash/共享 VM）、清理诊断日志。
