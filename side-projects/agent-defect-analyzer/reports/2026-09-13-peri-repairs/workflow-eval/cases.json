{
  "schemaVersion": 1,
  "cases": [
    {
      "id": "readme-review",
      "request": "用 Workflow 启动一个只读子任务，检查 README 的安装步骤是否清楚，返回它的总结。"
    },
    {
      "id": "parallel-inspection",
      "request": "用 Workflow 并行安排两个只读子任务，一个检查错误处理，一个检查取消传播，返回两个结果。"
    },
    {
      "id": "config-investigation",
      "request": "用 Workflow 让一个子任务读取项目的 package.json，说明测试命令。保持项目不变，返回总结。"
    },
    {
      "id": "nested-workspace",
      "request": "本次 Workflow 的工作目录在仓库子目录。让一个只读子任务检查这里的接口定义并返回发现，不修改任何文件。"
    }
  ]
}
