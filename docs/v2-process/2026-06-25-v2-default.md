# 2026-06-25 快照：v2 切默认完成

**日期**：2026-06-25
**分支**：`feature/v2-architecture`
**基线**：默认（v2）与 `PERI_USE_V1=1`（v1 回退）双路径测试等价 —— 数字见下方验证段
**vs Stage 3 基线**：2927 passed 保持，零回归

---

## 1. 决策

Stage 1（紧急修复 Top 1/4/5/8）+ Stage 2（高危修复 Top 2/3/6/7）+ Stage 3（遥测修复 Top 9/10）全部落地，workflow 审计的 16 个 confirmed findings 全部修复。2927 测试双路径等价，主流程独立 grep 验证无 hallucination。

翻转 `PERI_USE_V2` 默认值：v2 stages 现为默认主路径，`PERI_USE_V1=1` 回退到 v1 ReActAgent executor。

## 2. 实现（3 文件改动）

### peri-acp/src/session/executor.rs::run_session_loop（line 1109-1111）

OLD:
```
    // P3 v2 路径开关：PERI_USE_V2=1 启用 v2 stages 驱动的 ReAct 循环。
    // 默认走 v1，确保未启用时行为完全一致。
    if std::env::var("PERI_USE_V2").is_ok() {
```

NEW:
```
    // v2 stages 路径现为默认（2026-06-25 翻转，Stage 1-3 + 切默认完成）。
    // 回退到 v1 ReActAgent executor：设置 PERI_USE_V1=1（用于 dogfood 期间临时比对）。
    if std::env::var("PERI_USE_V1").is_err() {
```

> 注：初始写为 `!var("PERI_USE_V1").is_ok()`，clippy `nonminimal_bool` 提示后改为等价的 `is_err()`。

### peri-acp/src/session/mod.rs（line 62）

注释更新：`PERI_USE_V2=1 路径用` → `v2 默认路径用；PERI_USE_V1=1 回退时不参与`

### CLAUDE.md（4 处）

- 当前状态：`v2 主路径就位（opt-in）` → `v2 stages 已成为默认主路径`
- P3 行：`PERI_USE_V2=1 切换` → `PERI_USE_V1=1 回退`
- 测试行：`PERI_USE_V2=1 下测试同样全过` → `PERI_USE_V1=1 回退测试同样全过`
- "启用 v2 路径" 段落 → "回退 v1 路径" 段落

## 3. 验证

```
cargo build --workspace                                    ✅
cargo test --workspace --lib                               ✅ 2927 passed / 0 failed / 4 ignored（v2 默认）
PERI_USE_V1=1 cargo test --workspace --lib                 ✅ 2927 passed / 0 failed / 4 ignored（v1 回退）
cargo clippy --workspace --all-targets -- -D warnings      ✅ 零 warning
cargo fmt --all -- --check                                 ✅ 零 diff
```

## 4. dogfood 计划

- 开发者本地默认走 v2 至少 1 周
- 发现回归时临时 `PERI_USE_V1=1` 回退
- 严重问题回滚：`git revert` 切默认 commit，无需改动其他

## 5. 下一步

P5 物理删除 v1（独立里程碑，4-6 周）：
- P5.1 SubAgent tool 迁移到 v2 stages
- P5.2 Hook executor 迁移
- P5.3 抽取 build_agent_components
- P5.4 91 测试迁移
- P5.5 物理删除 v1 死代码

详见 `roadmap.md` §P5。

## 6. 索引

- 仓库根 `CLAUDE.md` —— 项目工作守则（已更新 v2 架构状态段落）
- `docs/v2-process/2026-06-25-stage3-complete.md` —— Stage 3 快照（前置）
- `docs/v2-process/roadmap.md` —— 剩余路线（dogfood + P5）
- 本快照（`2026-06-25-v2-default.md`）—— v2 切默认完成

---

## 后续更新（P5 进行中，2026-06-25）

v1 已物理删除（commit 见 git log），本快照中提到的 `PERI_USE_V1=1` 回退路径不再可用。
具体删除范围与剩余人工任务见 `p5-v1-removal-checklist.md`。
