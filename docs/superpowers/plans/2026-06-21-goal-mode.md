# Goal 模式 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Goal 模式——agent 通过单一 Goal 工具创建长程目标，自驱循环执行直到完成或阻塞，complete 经 LLM 二元验证。

**Architecture:** 扩展 BaseTool trait 加 ToolContext（只读 messages + cwd）；新增 GoalController trait（读写 goal_state）；Goal 工具是 deferred tool，通过 action 参数分发 create/complete/block/get；GoalMiddleware 只实现 after_agent 钩子，复用 block_continue 机制让 agent 自驱续跑；complete action 复用 auxiliary_model（原 compact_model 改名）做 LLM 二元验证。

**Tech Stack:** Rust 2021, tokio async/await + async-trait, parking_lot::RwLock, serde_json, chrono, uuid

**Spec:** `docs/superpowers/specs/2026-06-21-goal-mode-design.md`

---

## File Structure

### 新建文件

| 文件 | 职责 |
|------|------|
| `peri-agent/src/goal/controller.rs` | GoalController trait 定义（读写接口） |
| `peri-middlewares/src/goal/tool.rs` | Goal 工具实现（单一工具，action 分发） |
| `peri-middlewares/src/goal/mod.rs` | goal 模块入口 |
| `peri-middlewares/src/skills/builtin/skills/goal/SKILL.md` | /goal builtin skill 指导文档 |

### 修改文件

| 文件 | 变更 |
|------|------|
| `peri-agent/src/tools/mod.rs` | BaseTool trait 扩展 + ToolContext 定义 |
| `peri-agent/src/goal/mod.rs` | 导出 GoalController |
| `peri-agent/src/goal/model.rs` | GoalStatus 简化（砍 Paused/BudgetLimited） |
| `peri-agent/src/agent/executor/tool_dispatch.rs` | invoke 调用点加 ctx |
| `peri-agent/src/agent/executor/tool_setup.rs` | ToolWrapper invoke 转发 |
| `peri-acp/src/session/goal_state/mod.rs` | 实现 GoalController |
| `peri-acp/src/session/agent_pool.rs` | compact_model → auxiliary_model |
| `peri-acp/src/session/executor.rs` | compact_model 引用改名 |
| `peri-acp/src/session/command/mod.rs` | compact_model 引用改名 |
| `peri-acp/src/session/command/compact/pipeline.rs` | compact_model 引用改名 |
| `peri-acp/src/agent/builder.rs` | auxiliary_model 改名 + GoalMiddleware 注册 |
| `peri-middlewares/src/goal_middleware.rs` | 砍 before_model，重写 after_agent |
| `peri-middlewares/src/tool_search/execute_tool.rs` | 转发 ctx |
| `peri-middlewares/src/tools/mod.rs` | BoxToolWrapper/ArcToolWrapper 转发 ctx |
| `peri-middlewares/src/skills/builtin/mod.rs` | BUILTIN_SKILLS 注册 goal |
| 53 个 BaseTool 实现文件 | invoke 签名加 `ctx: ToolContext<'_>` |

---

## Phase 1: ToolContext 基础设施

> 此 Phase 为纯重构，无功能变更。完成后 `cargo build` + `cargo test` 全过即可独立提交。

### Task 1: 定义 ToolContext + 改 BaseTool trait

**Files:**
- Modify: `peri-agent/src/tools/mod.rs`

- [ ] **Step 1: 在 `BaseTool` trait 定义前新增 `ToolContext` 结构体**

在 `peri-agent/src/tools/mod.rs` 的 `ToolDefinition` struct 定义之后、`BaseTool` trait 之前，插入：

```rust
/// 工具只读上下文（借用 state，零 clone）
///
/// 通过 `BaseTool::invoke` 的第二个参数传入。工具可读取 messages 和 cwd，
/// 但不能修改 state（避免绕过 dispatch_tools 统一写入语义）。
pub struct ToolContext<'a> {
    /// 当前对话历史（只读引用，借用 state.messages）
    pub messages: &'a [crate::messages::BaseMessage],
    /// 当前工作目录
    pub cwd: &'a str,
}

impl<'a> ToolContext<'a> {
    pub fn new(messages: &'a [crate::messages::BaseMessage], cwd: &'a str) -> Self {
        Self { messages, cwd }
    }
}
```

- [ ] **Step 2: 修改 `BaseTool::invoke` 签名**

将：
```rust
async fn invoke(
    &self,
    input: serde_json::Value,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>>;
```

改为：
```rust
async fn invoke(
    &self,
    input: serde_json::Value,
    ctx: ToolContext<'_>,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>>;
```

- [ ] **Step 3: 验证编译报错（预期 53 个实现签名不匹配）**

Run: `cargo build -p peri-agent 2>&1 | head -20`
Expected: 编译错误（trait 方法签名变了，实现还没改）

- [ ] **Step 4: Commit**

```bash
git add peri-agent/src/tools/mod.rs
git commit -m "refactor: extend BaseTool::invoke with ToolContext parameter"
```

### Task 2: 修改 Wrapper 工具转发 ctx

**Files:**
- Modify: `peri-middlewares/src/tools/mod.rs:30-71` (BoxToolWrapper + ArcToolWrapper)
- Modify: `peri-agent/src/agent/executor/tool_setup.rs` (ToolWrapper)

- [ ] **Step 1: 修改 `BoxToolWrapper::invoke`**

`peri-middlewares/src/tools/mod.rs`，将 BoxToolWrapper 的 invoke：

```rust
async fn invoke(
    &self,
    input: serde_json::Value,
    ctx: peri_agent::tools::ToolContext<'_>,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
    self.0.invoke(input, ctx).await
}
```

- [ ] **Step 2: 修改 `ArcToolWrapper::invoke`**

同文件，ArcToolWrapper 的 invoke 同理：

```rust
async fn invoke(
    &self,
    input: serde_json::Value,
    ctx: peri_agent::tools::ToolContext<'_>,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
    self.0.invoke(input, ctx).await
}
```

- [ ] **Step 3: 修改 `ToolWrapper::invoke`**

`peri-agent/src/agent/executor/tool_setup.rs`，ToolWrapper（ManuallyDrop wrapper）的 invoke：

```rust
async fn invoke(
    &self,
    input: serde_json::Value,
    ctx: crate::tools::ToolContext<'_>,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
    self.inner().invoke(input, ctx).await
}
```

> 注意：先读 `tool_setup.rs` 确认 `self.inner()` 的确切调用方式，适配现有代码。

- [ ] **Step 4: Commit**

```bash
git add peri-middlewares/src/tools/mod.rs peri-agent/src/agent/executor/tool_setup.rs
git commit -m "refactor: forward ToolContext in wrapper tools"
```

### Task 3: 修改 tool_dispatch 调用点构造 ctx

**Files:**
- Modify: `peri-agent/src/agent/executor/tool_dispatch.rs:330-360`

- [ ] **Step 1: 在 invoke 调用前构造 ToolContext**

找到 `tool_dispatch.rs` 中 `t.invoke(input).await` 的位置（约 344 行），改为：

```rust
let ctx = crate::tools::ToolContext::new(state.messages(), &state.cwd);
match tool {
    Some(t) => t.invoke(input, ctx).await.map_err(|e| {
        AgentError::ToolExecutionFailed {
            tool: tool_name.clone(),
            reason: e.to_string(),
        }
    }),
    None => Err(AgentError::ToolNotFound(tool_name.clone())),
}
```

> **注意**：此闭包捕获了 `state` 的不可变引用。检查 `collect_tool_results` 函数签名，确认 `state` 在闭包作用域内可用。如果借用冲突，在闭包前提取 `let messages = state.messages(); let cwd = state.cwd().clone();` 再构造 ctx。

- [ ] **Step 2: Commit**

```bash
git add peri-agent/src/agent/executor/tool_dispatch.rs
git commit -m "refactor: pass ToolContext to tool invoke in tool_dispatch"
```

### Task 4: 修改 ExecuteExtraTool 转发 ctx

**Files:**
- Modify: `peri-middlewares/src/tool_search/execute_tool.rs:65-95`

- [ ] **Step 1: 修改 ExecuteExtraTool::invoke 签名 + 转发 ctx**

将 invoke 方法改为：

```rust
async fn invoke(
    &self,
    input: Value,
    ctx: peri_agent::tools::ToolContext<'_>,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
    let tool_name = input
        .get(EXTRA_TOOL_NAME_FIELD)
        .and_then(|v| v.as_str())
        .ok_or(format!(
            "{}: missing required '{}' parameter",
            EXECUTE_EXTRA_TOOL_NAME, EXTRA_TOOL_NAME_FIELD
        ))?;

    let params = input
        .get(EXTRA_TOOL_PARAMS_FIELD)
        .ok_or(format!(
            "{}: missing required '{}' parameter",
            EXECUTE_EXTRA_TOOL_NAME, EXTRA_TOOL_PARAMS_FIELD
        ))?
        .clone();

    let tool = {
        let tools = self.shared_tools.read();
        tools.get(tool_name).cloned().ok_or(format!(
            "{}: tool '{}' not found or not registered as a deferred tool",
            EXECUTE_EXTRA_TOOL_NAME, tool_name
        ))?
    };

    let result = tool.invoke(params, ctx).await?;
    Ok(result)
}
```

- [ ] **Step 2: Commit**

```bash
git add peri-middlewares/src/tool_search/execute_tool.rs
git commit -m "refactor: forward ToolContext in ExecuteExtraTool"
```

### Task 5: 批量修改 peri-middlewares 生产工具

> 以下 24 个生产工具的 invoke 签名修改完全一致。模式：`input: Value` 后加 `, ctx: peri_agent::tools::ToolContext<'_>`。不需要读 ctx 的工具用 `_ctx`。

**Files (生产工具):**
- `peri-middlewares/src/tools/todo.rs` — TodoWriteTool
- `peri-middlewares/src/tools/ask_user_tool.rs` — AskUserTool
- `peri-middlewares/src/tools/filesystem/glob.rs` — GlobFilesTool
- `peri-middlewares/src/tools/filesystem/write.rs` — WriteFileTool
- `peri-middlewares/src/tools/filesystem/edit.rs` — EditFileTool
- `peri-middlewares/src/tools/filesystem/grep.rs` — GrepTool
- `peri-middlewares/src/tools/filesystem/read.rs` — ReadFileTool
- `peri-middlewares/src/tools/filesystem/folder.rs` — FolderOperationsTool
- `peri-middlewares/src/middleware/terminal.rs` — BashTool
- `peri-middlewares/src/middleware/web_search.rs` — WebSearchTool
- `peri-middlewares/src/middleware/web_fetch.rs` — WebFetchTool
- `peri-middlewares/src/lsp/tool.rs` — LspTool
- `peri-middlewares/src/cron/tools.rs` — CronRegisterTool, CronListTool, CronRemoveTool
- `peri-middlewares/src/subagent/tool/define.rs` — SubAgentTool
- `peri-middlewares/src/subagent/agent_result.rs` — AgentResultTool
- `peri-middlewares/src/tool_search/search_tool.rs` — SearchExtraTools
- `peri-middlewares/src/tool_search/artifact_tool.rs` — ArtifactTool
- `peri-middlewares/src/mcp/tool_bridge.rs` — McpToolBridge
- `peri-middlewares/src/mcp/resource_tool.rs` — McpResourceTool

- [ ] **Step 1: 逐文件修改 invoke 签名**

每个工具的 `async fn invoke` 方法，将签名从：

```rust
async fn invoke(
    &self,
    input: serde_json::Value,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
```

改为（不使用 ctx 的工具）：

```rust
async fn invoke(
    &self,
    input: serde_json::Value,
    _ctx: peri_agent::tools::ToolContext<'_>,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
```

或改为（使用 ctx 的工具，目前无生产工具需要读 ctx，全部用 `_ctx`）：

```rust
async fn invoke(
    &self,
    input: serde_json::Value,
    ctx: peri_agent::tools::ToolContext<'_>,
) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
```

> **执行提示**：这是机械重复工作。可以用 sed 批量替换：
> ```bash
> # 先确认每个文件的 invoke 签名格式一致
> grep -rn "async fn invoke" peri-middlewares/src/tools/ peri-middlewares/src/middleware/ peri-middlewares/src/lsp/ peri-middlewares/src/cron/ peri-middlewares/src/subagent/ peri-middlewares/src/tool_search/ peri-middlewares/src/mcp/
> ```
> 然后逐文件用 Edit 工具修改（不用 sed，因为 Edit 更安全）。

- [ ] **Step 2: 构建验证**

Run: `cargo build -p peri-middlewares 2>&1 | grep "error" | head -20`
Expected: 编译通过或仅剩测试文件报错

- [ ] **Step 3: Commit**

```bash
git add peri-middlewares/src/
git commit -m "refactor: add ToolContext to peri-middlewares production tools"
```

### Task 6: 批量修改 peri-agent 测试工具

**Files (测试工具):**
- `peri-agent/tests/integration_tests.rs` — CounterTool
- `peri-agent/tests/agent_tests.rs` — OverrideEchoTool, EchoTool, FailingTool
- `peri-agent/examples/tool_integration.rs` — CalculatorTool
- `peri-agent/src/agent/executor/tool_dispatch_test.rs` — MockEchoTool, FailToolB, SlowTool, EchoTool, EchoAgent, AlwaysFailRead, FastTool, SlowTool, FailTool
- `peri-agent/src/agent/executor/mod_test.rs` — SlowTool, HangingTool, EchoTool(x4), GhostTool

- [ ] **Step 1: 逐文件修改 invoke 签名**

同 Task 5 模式。每个测试工具的 invoke 加 `_ctx: crate::tools::ToolContext<'_>`（peri-agent 内部用 `crate::` 前缀）或 `peri_agent::tools::ToolContext<'_>`（测试目录用完整路径）。

- [ ] **Step 2: 构建验证**

Run: `cargo test -p peri-agent --no-run 2>&1 | grep "error" | head -20`
Expected: 编译通过

- [ ] **Step 3: Commit**

```bash
git add peri-agent/tests/ peri-agent/examples/ peri-agent/src/agent/executor/
git commit -m "refactor: add ToolContext to peri-agent test tools"
```

### Task 7: 批量修改 peri-middlewares 测试工具

**Files (测试工具):**
- `peri-middlewares/src/subagent/tool/tool_test.rs` — DummyTool
- `peri-middlewares/src/subagent/fork_test.rs` — DummyTool
- `peri-middlewares/src/tool_search/tool_index_test.rs` — MockTool
- `peri-middlewares/src/tool_search/search_tool_test.rs` — MockTool
- `peri-middlewares/src/tool_search/execute_tool_test.rs` — MockTool
- `peri-middlewares/src/tool_search/middleware_test.rs` — MockTool
- `peri-middlewares/tests/middleware_tool_registration.rs` — MockReadFile

- [ ] **Step 1: 逐文件修改 invoke 签名**

同 Task 5 模式。`_ctx: peri_agent::tools::ToolContext<'_>`。

- [ ] **Step 2: 全量构建 + 测试验证**

Run: `cargo build 2>&1 | grep -c "error"`
Expected: 0 (无错误)

Run: `cargo test 2>&1 | tail -20`
Expected: 全部测试通过

- [ ] **Step 3: Commit**

```bash
git add peri-middlewares/
git commit -m "refactor: add ToolContext to peri-middlewares test tools

Phase 1 complete: all 53 BaseTool implementations updated.
cargo build + cargo test all green."
```

---

## Phase 2: Goal 数据层

### Task 8: GoalStatus 简化

**Files:**
- Modify: `peri-agent/src/goal/model.rs`
- Modify: `peri-agent/src/goal/model_test.rs`

- [ ] **Step 1: 修改 GoalStatus 枚举**

将 `peri-agent/src/goal/model.rs` 中的 GoalStatus 枚举（约 13-24 行）改为：

```rust
/// Goal 状态机
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum GoalStatus {
    /// 活跃，continuation 可运行
    Active,
    /// Agent 宣告完成（终态）
    Complete,
    /// Agent 宣告阻塞（终态，必须附带 reason）
    Blocked,
}
```

- [ ] **Step 2: 修改 `can_transition_to` 方法**

```rust
impl GoalStatus {
    /// 检查状态转换是否合法
    pub fn can_transition_to(&self, target: &GoalStatus) -> bool {
        use GoalStatus::*;
        match (self, target) {
            // 终态不可转换
            (Complete, _) | (Blocked, _) => false,
            // Active → Complete / Blocked
            (Active, Complete | Blocked) => true,
            (Active, Active) => false,
        }
    }

    /// 是否是终态（continuation 应停止）
    pub fn is_terminal(&self) -> bool {
        use GoalStatus::*;
        matches!(self, Complete | Blocked)
    }
}
```

- [ ] **Step 3: 修改 Display 实现**

```rust
impl std::fmt::Display for GoalStatus {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        use GoalStatus::*;
        match self {
            Active => write!(f, "active"),
            Complete => write!(f, "complete"),
            Blocked => write!(f, "blocked"),
        }
    }
}
```

- [ ] **Step 4: 更新 model_test.rs**

删除所有引用 `Paused` 和 `BudgetLimited` 的测试用例，新增简化后的状态转换测试：

```rust
#[test]
fn test_status_transitions() {
    use GoalStatus::*;
    // Active → Complete / Blocked
    assert!(Active.can_transition_to(&Complete));
    assert!(Active.can_transition_to(&Blocked));
    assert!(!Active.can_transition_to(&Active));
    // 终态不可转
    assert!(!Complete.can_transition_to(&Active));
    assert!(!Complete.can_transition_to(&Blocked));
    assert!(!Blocked.can_transition_to(&Active));
    assert!(!Blocked.can_transition_to(&Complete));
}

#[test]
fn test_is_terminal() {
    use GoalStatus::*;
    assert!(!Active.is_terminal());
    assert!(Complete.is_terminal());
    assert!(Blocked.is_terminal());
}
```

- [ ] **Step 5: 验证**

Run: `cargo test -p peri-agent --lib -- goal::model 2>&1`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add peri-agent/src/goal/model.rs peri-agent/src/goal/model_test.rs
git commit -m "refactor: simplify GoalStatus to Active/Complete/Blocked"
```

### Task 9: GoalController trait 定义

**Files:**
- Create: `peri-agent/src/goal/controller.rs`
- Modify: `peri-agent/src/goal/mod.rs`

- [ ] **Step 1: 创建 GoalController trait**

新建 `peri-agent/src/goal/controller.rs`：

```rust
//! GoalController — goal 读写接口（供 Goal 工具和 GoalMiddleware 依赖注入）。
//!
//! 定义在 peri-agent 层避免 peri-middlewares → peri-acp 循环依赖。
//! peri-acp 的 GoalState 实现此 trait。

use async_trait::async_trait;

use super::model::GoalStatus;
use super::view::GoalViewSnapshot;

/// Goal 读写控制器接口
#[async_trait]
pub trait GoalController: Send + Sync {
    /// 创建 goal。如果 goal 已存在返回 Err。
    async fn create_goal(&self, objective: String) -> Result<(), String>;

    /// 声明完成。状态转换非法时返回 Err。
    async fn complete_goal(&self) -> Result<(), String>;

    /// 声明阻塞。reason 必填。状态转换非法时返回 Err。
    async fn block_goal(&self, reason: String) -> Result<(), String>;

    /// 只读快照（get action + after_agent 判断用）
    fn snapshot(&self) -> GoalViewSnapshot;
}

/// GoalController 的补充视图（after_agent 只需判断 active）
pub fn is_active(snap: &GoalViewSnapshot) -> bool {
    snap.status == Some(GoalStatus::Active)
}
```

- [ ] **Step 2: 在 mod.rs 导出**

`peri-agent/src/goal/mod.rs` 添加：

```rust
pub mod controller;

pub use controller::{is_active as is_goal_active, GoalController};
```

- [ ] **Step 3: 验证编译**

Run: `cargo build -p peri-agent 2>&1 | grep "error"`
Expected: 0 errors

- [ ] **Step 4: Commit**

```bash
git add peri-agent/src/goal/controller.rs peri-agent/src/goal/mod.rs
git commit -m "feat: add GoalController trait"
```

### Task 10: GoalState 实现 GoalController

**Files:**
- Modify: `peri-acp/src/session/goal_state/mod.rs`
- Modify: `peri-acp/src/session/goal_state/mod_test.rs`

- [ ] **Step 1: 为 GoalState 实现 GoalController**

在 `peri-acp/src/session/goal_state/mod.rs` 末尾（现有 `impl GoalStateView` 之后）添加：

```rust
#[async_trait]
impl peri_agent::goal::GoalController for GoalState {
    async fn create_goal(&self, objective: String) -> Result<(), String> {
        // 检查是否已有 goal
        {
            let guard = self.inner.read();
            if guard.goal.is_some() {
                return Err("goal 已存在，请先 clear 后重建".to_string());
            }
        }
        // set_goal 内部会设 objective_just_updated = true（保留兼容）
        self.set_goal(objective, None)
            .await
            .map_err(|e| e.to_string())
    }

    async fn complete_goal(&self) -> Result<(), String> {
        self.set_status(GoalStatus::Complete).await
    }

    async fn block_goal(&self, reason: String) -> Result<(), String> {
        self.set_status_with_reason(GoalStatus::Blocked, reason).await
    }

    fn snapshot(&self) -> peri_agent::goal::GoalViewSnapshot {
        peri_agent::goal::GoalStateView::snapshot(self)
    }
}
```

- [ ] **Step 2: 添加测试**

在 `mod_test.rs` 中添加 GoalController 实现的测试：

```rust
#[tokio::test]
async fn test_goal_controller_create_duplicate_errors() {
    let store = Arc::new(InMemoryGoalStore::new()) as Arc<dyn GoalStore>;
    let state = GoalState::new(store, "thread-1".to_string());

    // 第一次 create 成功
    state
        .create_goal("测试目标".to_string())
        .await
        .unwrap();

    // 第二次 create 报错
    let result = state
        .create_goal("另一个目标".to_string())
        .await;
    assert!(result.is_err());
    assert!(result.unwrap_err().contains("已存在"));
}

#[tokio::test]
async fn test_goal_controller_complete() {
    let store = Arc::new(InMemoryGoalStore::new()) as Arc<dyn GoalStore>;
    let state = GoalState::new(store, "thread-1".to_string());

    state
        .create_goal("测试目标".to_string())
        .await
        .unwrap();
    state.complete_goal().await.unwrap();

    let snap = state.snapshot();
    assert_eq!(snap.status, Some(GoalStatus::Complete));
}

#[tokio::test]
async fn test_goal_controller_block_requires_reason() {
    let store = Arc::new(InMemoryGoalStore::new()) as Arc<dyn GoalStore>;
    let state = GoalState::new(store, "thread-1".to_string());

    state
        .create_goal("测试目标".to_string())
        .await
        .unwrap();
    state
        .block_goal("缺权限".to_string())
        .await
        .unwrap();

    let snap = state.snapshot();
    assert_eq!(snap.status, Some(GoalStatus::Blocked));
}

#[tokio::test]
async fn test_goal_controller_terminal_cannot_transition() {
    let store = Arc::new(InMemoryGoalStore::new()) as Arc<dyn GoalStore>;
    let state = GoalState::new(store, "thread-1".to_string());

    state
        .create_goal("测试目标".to_string())
        .await
        .unwrap();
    state.complete_goal().await.unwrap();

    // 终态不能再转
    let result = state.complete_goal().await;
    assert!(result.is_err());
}
```

- [ ] **Step 3: 验证**

Run: `cargo test -p peri-acp --lib -- goal_state 2>&1`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add peri-acp/src/session/goal_state/
git commit -m "feat: implement GoalController for GoalState"
```

---

## Phase 3: Goal 工具

### Task 11: compact_model → auxiliary_model 改名

**Files:**
- Modify: `peri-acp/src/session/agent_pool.rs`
- Modify: `peri-acp/src/session/executor.rs` (多处)
- Modify: `peri-acp/src/session/command/mod.rs`
- Modify: `peri-acp/src/session/command/compact/pipeline.rs`
- Modify: `peri-acp/src/agent/builder.rs`
- Modify: 相关测试文件

- [ ] **Step 1: 改名 CachedLlmInstances 字段**

`peri-acp/src/session/agent_pool.rs:24-33`，将 `compact_model` 改为 `auxiliary_model`：

```rust
#[derive(Clone)]
pub struct CachedLlmInstances {
    /// 辅助 LLM（CompactMiddleware 摘要 + Goal 工具验证共用）。
    /// Contains reqwest Client with connection pool.
    pub auxiliary_model: Arc<dyn BaseModel>,
    /// auto_classifier LLM (used by HITL HumanInTheLoopMiddleware).
    pub auto_classifier_model: Arc<tokio::sync::Mutex<Box<dyn BaseModel>>>,
    /// Provider fingerprint at time of creation.
    pub fingerprint: String,
}
```

- [ ] **Step 2: 全局替换引用**

在以下文件中将 `.compact_model` 替换为 `.auxiliary_model`：
- `peri-acp/src/session/executor.rs`（行 363, 460, 563, 971）
- `peri-acp/src/session/command/mod.rs`（行 40）
- `peri-acp/src/session/command/compact/pipeline.rs`（行 119-129）
- `peri-acp/src/agent/builder.rs`（行 621）
- 相关测试文件（compact_test.rs, mod_test.rs, rewind_test.rs, bg_test.rs）

> 执行提示：先 grep 所有引用确认完整列表
> ```bash
> grep -rn "compact_model" peri-acp/src/
> ```

- [ ] **Step 3: 验证编译**

Run: `cargo build -p peri-acp 2>&1 | grep "error"`
Expected: 0 errors

- [ ] **Step 4: Commit**

```bash
git add peri-acp/src/
git commit -m "refactor: rename compact_model to auxiliary_model"
```

### Task 12: Goal 工具实现

**Files:**
- Create: `peri-middlewares/src/goal/mod.rs`
- Create: `peri-middlewares/src/goal/tool.rs`
- Create: `peri-middlewares/src/goal/tool_test.rs`
- Modify: `peri-middlewares/src/lib.rs`（注册 goal 模块）

- [ ] **Step 1: 创建 goal 模块入口**

`peri-middlewares/src/goal/mod.rs`：

```rust
//! Goal 子系统中间件模块

pub mod tool;

pub use tool::GoalTool;
```

- [ ] **Step 2: 创建 Goal 工具**

`peri-middlewares/src/goal/tool.rs`：

```rust
//! Goal 工具 — 单一 deferred 工具，通过 action 参数分发。
//!
//! action: create / complete / block / get
//! complete 经 auxiliary_model LLM 二元验证。

use std::sync::Arc;

use async_trait::async_trait;
use peri_agent::tools::{BaseTool, ToolContext};
use peri_agent::{goal::GoalController, messages::BaseMessage};
use serde_json::{json, Value};

/// Goal 工具
pub struct GoalTool {
    controller: Arc<dyn GoalController>,
    /// 辅助 LLM（complete 验证用），None 时跳过验证
    auxiliary_model: Option<Arc<dyn peri_agent::model::BaseModel>>,
}

impl GoalTool {
    pub fn new(
        controller: Arc<dyn GoalController>,
        auxiliary_model: Option<Arc<dyn peri_agent::model::BaseModel>>,
    ) -> Self {
        Self {
            controller,
            auxiliary_model,
        }
    }

    const DESCRIPTION: &'static str = "长程目标跟踪工具。通过 action 参数区分操作：\n\
- create: 创建目标（objective 必填）。单个会话只能有一个 goal。\n\
- complete: 声明目标完成（经 LLM 验证，未通过会返回原因）\n\
- block: 声明遇到无法解决的阻塞（reason 必填）\n\
- get: 查询当前目标状态";

    async fn handle_create(
        objective: &str,
        controller: &dyn GoalController,
    ) -> Result<String, String> {
        controller
            .create_goal(objective.to_string())
            .await
            .map(|()| {
                format!(
                    "目标已创建: {objective}\n\n\
                     请围绕此目标持续推进。完成时调用 goal(complete)，\
                     遇到阻塞时调用 goal(block, reason)。"
                )
            })
    }

    async fn handle_complete(
        &self,
        ctx: &ToolContext<'_>,
    ) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
        let snap = self.controller.snapshot();
        let objective = match &snap.objective {
            Some(o) => o.clone(),
            None => return Ok("无活跃 goal，无法 complete".to_string()),
        };

        // auxiliary_model 为 None 时跳过验证
        if let Some(model) = &self.auxiliary_model {
            let user_content = Self::build_verify_prompt(&objective, ctx.messages);
            let request = peri_agent::model::LlmRequest::new(vec![BaseMessage::human(user_content)])
                .with_system(Self::VERIFY_SYSTEM_PROMPT.to_string())
                .with_max_tokens(1024);

            let response = model.invoke(request).await?;
            let raw = response.message.content();

            let verdict = Self::parse_verdict(raw);
            if !verdict.achieved {
                // 验证失败：goal 保持 Active
                return Ok(format!(
                    "目标未达成: {}\n请继续工作。",
                    verdict.missing
                ));
            }
            // 验证通过
            self.controller
                .complete_goal()
                .await
                .map_err(|e| -> Box<dyn std::error::Error + Send + Sync> {
                    e.into()
                })?;
            Ok(format!(
                "目标已完成。验证证据: {}",
                verdict.evidence
            ))
        } else {
            // 无 auxiliary_model，跳过验证直接完成
            self.controller
                .complete_goal()
                .await
                .map_err(|e| -> Box<dyn std::error::Error + Send + Sync> {
                    e.into()
                })?;
            Ok("目标已完成（跳过验证，未配置辅助 LLM）。".to_string())
        }
    }

    async fn handle_block(
        &self,
        reason: &str,
    ) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
        self.controller
            .block_goal(reason.to_string())
            .await
            .map_err(|e| -> Box<dyn std::error::Error + Send + Sync> {
                e.into()
            })?;
        Ok(format!("目标已标记为阻塞: {reason}"))
    }

    fn handle_get(controller: &dyn GoalController) -> String {
        let snap = controller.snapshot();
        match (&snap.objective, snap.status) {
            (Some(obj), Some(status)) => {
                format!("目标: {obj}\n状态: {status}\n已用: {} tokens", snap.tokens_used)
            }
            _ => "当前无目标。".to_string(),
        }
    }

    const VERIFY_SYSTEM_PROMPT: &'static str = "你是目标完成度评估器。判断 agent 是否达成了用户设定的目标。\n\
严格评估——只有确凿证据表明目标已达成才判 true。\n\n\
请输出 JSON 格式:\n\
{\"achieved\": true/false, \"evidence\": \"支撑判断的证据\", \"missing\": \"如未达成，还缺什么\"}";

    fn build_verify_prompt(objective: &str, messages: &[BaseMessage]) -> String {
        let transcript: Vec<String> = messages
            .iter()
            .map(|m| format!("[{}] {}", m.role_label(), m.content_summary()))
            .collect();
        format!(
            "目标: {objective}\n\n对话历史:\n{}\n\n请判断目标是否已达成。",
            transcript.join("\n")
        )
    }

    fn parse_verdict(raw: &str) -> GoalVerdict {
        // 尝试从 LLM 输出中提取 JSON
        // 宽松解析：找第一个 { 到最后一个 }
        let start = raw.find('{');
        let end = raw.rfind('}');
        if let (Some(s), Some(e)) = (start, end) {
            if let Ok(v) = serde_json::from_str::<Value>(&raw[s..=e]) {
                return GoalVerdict {
                    achieved: v.get("achieved").and_then(|a| a.as_bool()).unwrap_or(false),
                    evidence: v.get("evidence").and_then(|e| e.as_str()).unwrap_or("").to_string(),
                    missing: v.get("missing").and_then(|m| m.as_str()).unwrap_or("未提供原因").to_string(),
                };
            }
        }
        // 解析失败，默认未达成
        GoalVerdict {
            achieved: false,
            evidence: String::new(),
            missing: "验证 LLM 输出解析失败".to_string(),
        }
    }
}

struct GoalVerdict {
    achieved: bool,
    evidence: String,
    missing: String,
}

#[async_trait]
impl BaseTool for GoalTool {
    fn name(&self) -> &str {
        "goal"
    }

    fn description(&self) -> &str {
        Self::DESCRIPTION
    }

    fn parameters(&self) -> Value {
        json!({
            "type": "object",
            "properties": {
                "action": {
                    "type": "string",
                    "enum": ["create", "complete", "block", "get"],
                    "description": "操作类型"
                },
                "objective": {
                    "type": "string",
                    "description": "create 时必填。目标描述，需具体可验证。"
                },
                "reason": {
                    "type": "string",
                    "description": "block 时必填。阻塞原因。"
                }
            },
            "required": ["action"]
        })
    }

    async fn invoke(
        &self,
        input: Value,
        ctx: ToolContext<'_>,
    ) -> Result<String, Box<dyn std::error::Error + Send + Sync>> {
        let action = input
            .get("action")
            .and_then(|v| v.as_str())
            .ok_or("goal: missing required 'action' parameter")?;

        match action {
            "create" => {
                let objective = input
                    .get("objective")
                    .and_then(|v| v.as_str())
                    .ok_or("goal: create requires 'objective' parameter")?;
                Self::handle_create(objective, self.controller.as_ref())
                    .await
                    .map_err(|e| -> Box<dyn std::error::Error + Send + Sync> { e.into() })
            }
            "complete" => {
                self.handle_complete(&ctx).await
            }
            "block" => {
                let reason = input
                    .get("reason")
                    .and_then(|v| v.as_str())
                    .ok_or("goal: block requires 'reason' parameter")?;
                self.handle_block(reason).await
            }
            "get" => {
                Ok(Self::handle_get(self.controller.as_ref()))
            }
            other => Err(format!("goal: unknown action '{other}'").into()),
        }
    }
}
```

> **API 确认**：
> - `BaseMessage::content_summary()` 和 `BaseMessage::role_label()` 方法可能不存在。需要确认 BaseMessage 的现有方法，或用 `m.content()` + `format!("{:?}", m.role())` 替代。
> - `LlmRequest` 的确切 API 需要确认 `peri-agent/src/model/` 中的定义。
> - `peri_agent::model::BaseModel` 的确切路径需要确认。

- [ ] **Step 3: 确认 BaseMessage 和 LlmRequest API**

读取以下文件确认 API：
- `peri-agent/src/messages/mod.rs` — 确认 BaseMessage 有哪些方法（content, role 等）
- `peri-agent/src/model/mod.rs` 或 `peri-agent/src/model/` — 确认 LlmRequest 和 BaseModel 的确切签名

根据实际 API 调整 `build_verify_prompt` 和 `handle_complete` 的代码。

- [ ] **Step 4: 注册 goal 模块**

`peri-middlewares/src/lib.rs` 添加：
```rust
pub mod goal;
```

- [ ] **Step 5: 创建测试文件**

`peri-middlewares/src/goal/tool_test.rs`：

```rust
use super::*;
use async_trait::async_trait;
use peri_agent::goal::{GoalController, GoalStatus, GoalViewSnapshot};

struct MockController {
    has_goal: parking_lot::Mutex<bool>,
}

impl MockController {
    fn new() -> Self {
        Self {
            has_goal: parking_lot::Mutex::new(false),
        }
    }
}

#[async_trait]
impl GoalController for MockController {
    async fn create_goal(&self, objective: String) -> Result<(), String> {
        let mut guard = self.has_goal.lock();
        if *guard {
            return Err("goal 已存在".to_string());
        }
        *guard = true;
        let _ = objective;
        Ok(())
    }

    async fn complete_goal(&self) -> Result<(), String> {
        Ok(())
    }

    async fn block_goal(&self, _reason: String) -> Result<(), String> {
        Ok(())
    }

    fn snapshot(&self) -> GoalViewSnapshot {
        let guard = self.has_goal.lock();
        if *guard {
            GoalViewSnapshot {
                objective: Some("测试目标".to_string()),
                status: Some(GoalStatus::Active),
                token_budget: None,
                tokens_used: 0,
                objective_just_updated: false,
            }
        } else {
            GoalViewSnapshot::default()
        }
    }
}

#[tokio::test]
async fn test_goal_create() {
    let controller = Arc::new(MockController::new()) as Arc<dyn GoalController>;
    let tool = GoalTool::new(controller, None);

    let input = json!({"action": "create", "objective": "测试目标"});
    let ctx = ToolContext::new(&[], ".");
    let result = tool.invoke(input, ctx).await.unwrap();
    assert!(result.contains("目标已创建"));
}

#[tokio::test]
async fn test_goal_create_duplicate() {
    let controller = Arc::new(MockController::new()) as Arc<dyn GoalController>;
    let tool = GoalTool::new(Arc::clone(&controller), None);

    let input = json!({"action": "create", "objective": "目标1"});
    let ctx = ToolContext::new(&[], ".");
    tool.invoke(input.clone(), ctx).await.unwrap();

    let result = tool.invoke(input, ToolContext::new(&[], ".")).await.unwrap();
    assert!(result.contains("创建失败"));
}

#[tokio::test]
async fn test_goal_get_no_goal() {
    let controller = Arc::new(MockController::new()) as Arc<dyn GoalController>;
    let tool = GoalTool::new(controller, None);

    let input = json!({"action": "get"});
    let ctx = ToolContext::new(&[], ".");
    let result = tool.invoke(input, ctx).await.unwrap();
    assert!(result.contains("无目标"));
}

#[tokio::test]
async fn test_goal_block() {
    let controller = Arc::new(MockController::new()) as Arc<dyn GoalController>;
    let tool = GoalTool::new(controller, None);

    // 先 create
    tool.invoke(json!({"action": "create", "objective": "目标"}), ToolContext::new(&[], "."))
        .await
        .unwrap();

    let result = tool
        .invoke(json!({"action": "block", "reason": "测试阻塞"}), ToolContext::new(&[], "."))
        .await
        .unwrap();
    assert!(result.contains("阻塞"));
}

#[tokio::test]
async fn test_goal_complete_no_model_skips_verification() {
    let controller = Arc::new(MockController::new()) as Arc<dyn GoalController>;
    let tool = GoalTool::new(controller, None);

    tool.invoke(json!({"action": "create", "objective": "目标"}), ToolContext::new(&[], "."))
        .await
        .unwrap();

    let result = tool
        .invoke(json!({"action": "complete"}), ToolContext::new(&[], "."))
        .await
        .unwrap();
    assert!(result.contains("跳过验证"));
}
```

- [ ] **Step 6: 验证**

Run: `cargo test -p peri-middlewares --lib -- goal::tool 2>&1`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add peri-middlewares/src/goal/ peri-middlewares/src/lib.rs
git commit -m "feat: implement GoalTool with create/complete/block/get actions"
```

---

## Phase 4: GoalMiddleware

### Task 13: 重写 GoalMiddleware

**Files:**
- Modify: `peri-middlewares/src/goal_middleware.rs`
- Modify: `peri-middlewares/src/goal_middleware_test.rs`

- [ ] **Step 1: 重写 GoalMiddleware**

将 `peri-middlewares/src/goal_middleware.rs` 全部替换为：

```rust
//! GoalMiddleware — after_agent 钩子注入递增紧迫感 steering。
//!
//! goal active 时每轮注入提示 + 设 block_continue，executor 自动续跑。
//! agent 必须调 goal(complete) 或 goal(block) 才能终止循环。
//!
//! 注入路径：add_message(Human, <system-reminder>) 尾部追加。
//! 绝不破坏 frozen_system_prompt。

use std::sync::atomic::{AtomicUsize, Ordering};
use std::sync::Arc;

use async_trait::async_trait;
use peri_agent::{
    agent::state::State,
    error::AgentResult,
    messages::BaseMessage,
    middleware::r#trait::Middleware,
};

/// Goal steering 注入中间件（链最后）
pub struct GoalMiddleware {
    controller: Arc<dyn peri_agent::goal::GoalController>,
    /// 连续 after_agent 注入次数（递增紧迫感用）
    pending_rounds: AtomicUsize,
}

impl GoalMiddleware {
    pub fn new(controller: Arc<dyn peri_agent::goal::GoalController>) -> Self {
        Self {
            controller,
            pending_rounds: AtomicUsize::new(0),
        }
    }

    /// 渲染递增紧迫感模板
    fn render_steering(objective: &str, round: usize) -> String {
        let urgency = match round {
            1 => "你刚才给出了回答但未声明目标完成。请决策：\n\
                  - 已达成 → goal(complete)\n\
                  - 遇到阻塞 → goal(block, reason)\n\
                  - 还需继续 → 执行下一步",
            2 => "目标尚未完成。你必须调用 goal(complete) 或 goal(block) 来结束，或继续执行下一步。",
            _ => "注意：目标仍未完成。请立即决策——继续工作或声明终态。",
        };
        format!(
            "<system-reminder>\n\
             [Goal Steering]\n\
             目标: {objective}\n\
             {urgency}\n\
             </system-reminder>"
        )
    }
}

#[async_trait]
impl<S: State> Middleware<S> for GoalMiddleware {
    fn name(&self) -> &str {
        "GoalMiddleware"
    }

    fn collect_tools(&self, _cwd: &str) -> Vec<Box<dyn peri_agent::tools::BaseTool>> {
        // Goal 工具不通过 collect_tools 注册（它是 deferred tool）
        // 通过 builder 直接注入到 shared_tools 注册表
        vec![]
    }

    async fn after_agent(
        &self,
        state: &mut S,
        output: &peri_agent::agent::react::AgentOutput,
    ) -> AgentResult<peri_agent::agent::react::AgentOutput> {
        // 1. 前面已有 block_continue（如 HookMiddleware stop block）→ 不干预
        if output.block_continue.is_some() {
            return Ok(output.clone());
        }

        // 2. 检查 goal 状态
        let snap = self.controller.snapshot();
        if !peri_agent::goal::is_goal_active(&snap) {
            // 无 goal 或终态 → 重置计数器 + 放行
            self.pending_rounds.store(0, Ordering::Relaxed);
            return Ok(output.clone());
        }

        // 3. goal active → 注入递增紧迫感 steering
        let round = self.pending_rounds.fetch_add(1, Ordering::Relaxed) + 1;
        let objective = snap.objective.as_deref().unwrap_or("(未知)");
        let template = Self::render_steering(objective, round);
        state.add_message(BaseMessage::human(template));

        tracing::debug!(
            objective = %objective,
            round = round,
            "GoalMiddleware: 注入 after_agent steering"
        );

        // 4. 设 block_continue，executor 自动续跑
        let mut output = output.clone();
        output.block_continue = Some("goal_active".to_string());
        Ok(output)
    }
}

#[cfg(test)]
#[path = "goal_middleware_test.rs"]
mod tests;
```

- [ ] **Step 2: 重写测试**

`peri-middlewares/src/goal_middleware_test.rs`：

```rust
use super::*;
use peri_agent::agent::react::AgentOutput;
use peri_agent::goal::{GoalController, GoalStatus, GoalViewSnapshot};

// 复用 tool_test.rs 的 MockController 模式
struct MockController {
    snapshot: parking_lot::Mutex<GoalViewSnapshot>,
}

#[async_trait]
impl GoalController for MockController {
    async fn create_goal(&self, _objective: String) -> Result<(), String> { Ok(()) }
    async fn complete_goal(&self) -> Result<(), String> { Ok(()) }
    async fn block_goal(&self, _reason: String) -> Result<(), String> { Ok(()) }
    fn snapshot(&self) -> GoalViewSnapshot {
        self.snapshot.lock().clone()
    }
}

fn make_active_snapshot() -> GoalViewSnapshot {
    GoalViewSnapshot {
        objective: Some("测试目标".to_string()),
        status: Some(GoalStatus::Active),
        token_budget: None,
        tokens_used: 0,
        objective_just_updated: false,
    }
}

fn make_complete_snapshot() -> GoalViewSnapshot {
    GoalViewSnapshot {
        status: Some(GoalStatus::Complete),
        ..make_active_snapshot()
    }
}

#[test]
fn test_render_steering_escalates() {
    let r1 = GoalMiddleware::render_steering("目标", 1);
    assert!(r1.contains("请决策"));

    let r2 = GoalMiddleware::render_steering("目标", 2);
    assert!(r2.contains("必须"));

    let r3 = GoalMiddleware::render_steering("目标", 3);
    assert!(r3.contains("注意"));
}

// after_agent 集成测试需要 mock State，此处验证模板逻辑
// 完整集成测试在 Phase 4 集成验证中
```

- [ ] **Step 3: 验证**

Run: `cargo test -p peri-middlewares --lib -- goal_middleware 2>&1`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add peri-middlewares/src/goal_middleware.rs peri-middlewares/src/goal_middleware_test.rs
git commit -m "feat: rewrite GoalMiddleware with after_agent block_continue"
```

### Task 14: 注册 GoalMiddleware + Goal 工具到 builder.rs

**Files:**
- Modify: `peri-acp/src/agent/builder.rs`

- [ ] **Step 1: 在 builder 函数签名中添加 goal 参数**

找到 `build_agent` 函数（`builder.rs` 约 104-136 行的 `AcpAgentConfig`），添加 goal controller：

```rust
pub struct AcpAgentConfig {
    // ... 现有字段 ...
    /// Goal controller（None = 不启用 goal 功能）
    pub goal_controller: Option<Arc<dyn peri_agent::goal::GoalController>>,
    /// 辅助 LLM（compact 摘要 + goal 验证共用）
    // auxiliary_model 已在 compact 改名中处理
}
```

- [ ] **Step 2: 在中间件链末尾注册 GoalMiddleware**

找到 CompactMiddleware 注册后的位置（约 617 行），在最后添加：

```rust
// GoalMiddleware（链最后，在 CompactMiddleware 之后）
let executor = if let Some(controller) = req.goal_controller.clone() {
    let goal_mw = peri_middlewares::GoalMiddleware::new(controller);
    executor.add_middleware(Box::new(goal_mw))
} else {
    executor
};
```

- [ ] **Step 3: 将 Goal 工具注入 shared_tools 注册表**

在 shared_tools 填充逻辑附近（找到 shared_tools 的初始化代码），添加 Goal 工具：

```rust
// 注册 Goal 工具为 deferred tool
if let Some(controller) = &goal_controller {
    let goal_tool = peri_middlewares::goal::GoalTool::new(
        Arc::clone(controller),
        auxiliary_model.clone(), // 复用 auxiliary_model
    );
    shared_tools.write().insert(
        "goal".to_string(),
        Arc::new(goal_tool) as Arc<dyn BaseTool>,
    );
}
```

> **注意**：需要找到 shared_tools 的确切初始化位置。shared_tools 是 `Arc<RwLock<HashMap<String, Arc<dyn BaseTool>>>>`，在 builder.rs 中被 tool_search 和 execute_extra_tool 共享。

- [ ] **Step 4: 在调用方传递 goal_controller**

找到 `build_agent` 的调用方（`peri-acp/src/session/executor.rs` 的 `build_and_execute_agent`），从 AcpSession 中获取 goal_state 并传递。

```rust
// 在 BuildAgentRequest 中添加 goal_controller 字段
pub struct BuildAgentRequest<'a> {
    // ... 现有字段 ...
    pub goal_controller: Option<Arc<dyn peri_agent::goal::GoalController>>,
}
```

在 `build_and_execute_agent` 中：
```rust
// 从 session 获取 goal_state
let goal_controller: Option<Arc<dyn peri_agent::goal::GoalController>> = {
    // goal_state 实现了 GoalController，直接包 Arc
    // 需要确认 GoalState 是否 Clone（它是 Arc<RwLock<Inner>> 的 wrapper，Clone 是零成本）
    Some(Arc::new(req.session.goal_state.clone()))
};
```

> **注意**：`GoalState` 实现了 `Clone`（内部是 `Arc<RwLock<...>>`）。但 `GoalController` trait 需要被 `GoalState` 实现，而 `Arc::new(goal_state.clone())` 会创建 `Arc<GoalState>`，需要 `GoalState` 实现 `GoalController`（在 Task 10 已完成）。但 `Arc<GoalState>` 能否作为 `Arc<dyn GoalController>` 使用？需要 `GoalState: Sized + GoalController`。由于 `GoalState` 已经 `impl GoalController for GoalState`，可以直接 `Arc::new(goal_state.clone()) as Arc<dyn GoalController>`。

- [ ] **Step 5: 验证编译**

Run: `cargo build -p peri-acp 2>&1 | grep "error"`
Expected: 0 errors

- [ ] **Step 6: Commit**

```bash
git add peri-acp/src/agent/builder.rs peri-acp/src/session/executor.rs
git commit -m "feat: register GoalMiddleware and GoalTool in builder"
```

---

## Phase 5: Builtin Skill

### Task 15: 创建 /goal SKILL.md + 注册

**Files:**
- Create: `peri-middlewares/src/skills/builtin/skills/goal/SKILL.md`
- Modify: `peri-middlewares/src/skills/builtin/mod.rs`

- [ ] **Step 1: 创建 SKILL.md**

`peri-middlewares/src/skills/builtin/skills/goal/SKILL.md`：

```markdown
---
name: goal
description: >
  长程目标跟踪。当用户给出需要多步骤完成的复杂任务时使用。
  触发词："goal"、"目标"、"持续执行直到完成"、"直到 X 为止"。
userInvocable: true
argumentHint: "[objective description]"
---

# Goal 模式

## 何时使用

- 用户给出复杂任务，需要多轮执行才能完成
- 用户说"持续执行直到完成"、"不要中途停下"、"直到 X 为止"

## 如何使用

1. 调用 `goal` 工具，action=create，objective 设为具体可验证的目标描述
2. 持续工作，直到目标达成
3. 达成 → `goal` 工具，action=complete
4. 遇到无法解决的阻塞 → `goal` 工具，action=block，reason 填写阻塞原因
5. 随时可用 action=get 查询当前目标状态

## 重要约束

- **目标必须具体可验证**："优化代码"不好，"将测试覆盖率提到 80%"好
- **complete 会经验证**：系统会用辅助 LLM 判断你是否真的达成了目标，未通过会返回原因
- **block 是求救信号**：只在真正无法继续时使用（如缺权限、缺依赖）
- **创建后自驱**：goal 创建后，每轮结束时会收到提醒，你必须决策：继续/完成/阻塞
- **单例**：同一时间只能有一个 goal，创建新 goal 前需先 clear
```

- [ ] **Step 2: 注册到 BUILTIN_SKILLS**

`peri-middlewares/src/skills/builtin/mod.rs`，在 `BUILTIN_SKILLS` 数组追加：

```rust
pub static BUILTIN_SKILLS: &[BuiltinSkill] = &[
    BuiltinSkill {
        name: "use-artifacts",
        content: include_str!("skills/use-artifacts/SKILL.md"),
    },
    BuiltinSkill {
        name: "goal",
        content: include_str!("skills/goal/SKILL.md"),
    },
];
```

- [ ] **Step 3: 验证**

Run: `cargo test -p peri-middlewares --lib -- skills::builtin 2>&1`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add peri-middlewares/src/skills/builtin/
git commit -m "feat: add /goal builtin skill"
```

---

## 最终验证

- [ ] **全量构建**

Run: `cargo build 2>&1 | grep -c "error"`
Expected: 0

- [ ] **全量测试**

Run: `cargo test 2>&1 | tail -30`
Expected: 全部通过

- [ ] **手动验证（可选）**

启动 TUI，测试 goal 模式完整流程：
```bash
cargo run -p peri-tui
# 输入：帮我创建一个 goal，目标是确认项目能编译
# agent 应该调用 goal(create)，然后持续工作直到 complete
```
