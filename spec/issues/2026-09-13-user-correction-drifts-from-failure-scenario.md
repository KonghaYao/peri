# 用户纠正后修复场景漂移

**状态**：Open  
**优先级**：高  
**类型**：Agent 行为 / Prompt 约束 / 证据纪律  
**创建日期**：2026-09-13  
**来源**：本轮用户授权记录；观察队列 `PERI-20260913-CORRECTION-SCENARIO`

## 问题描述

当用户质疑一个具体机制或触发场景时，Agent 可能先把质疑扩展成未经证据支持的统一架构要求；用户随后纠正为更窄的触发时机或注入遗漏时，前一轮的架构解释会把修复目标带离原始故障。期望 Agent 先核对用户纠正后的可观察场景、已有证据和授权边界，再决定是修局部触发条件还是提出更大设计变更。

## 症状详情

- 研究证据定位：thread `01a07e86-72ed-7453-846a-edc2790c2402` 的 messages `01a07ec4-6185-72c2-b71e-aaf420d94fb7`、`01a07ec4-61c7-72f3-b907-985859a8c8c7`、`01a07ec7-6162-7090-9cd6-d175944baf40`、`01a07ed0-db46-76b1-8495-945e918e1545`。
- 审计样本中记录为 1 个 focal task；分母为 6 个 sampled sessions、4 个 engineering focal tasks，不能外推系统普遍发生率。
- 现有 `peri-acp/prompts/sections/03_doing_tasks.md:5-14` 要求暴露假设和歧义，`:40-46` 要求运行时问题依赖用户事实并在证据不足时询问。
- `peri-middlewares/src/skills/builtin/skills/ultra-task/SKILL.md:30-51` 要求先建立任务边界、验收要求和授权责任；`:132-151` 要求按验收证据收敛，并把未证实结果保留为风险。
- 观察队列另记录 45 个持久化 prefix 含 verifiable-goal instruction；这证明存在相关指导文本，不证明 provider 实际收到完整文本，也不证明指导导致或阻止了该次漂移。

## 现行观察与待验证假设

### 现行观察

- 单个采样任务中，用户对 cron 机制的质疑先被解释成统一架构要求，随后用户将问题纠正为触发时机未注入。
- 当前提示已包含“不要假设”和证据收敛要求，但没有针对“用户纠正后重新锁定故障场景”的显式检查点。
- 这是 Agent 行为审计观察，不是某一段 prompt 文本已被证明为唯一影响因素。

### 待验证假设

- 在用户纠正后要求 Agent 先复述可观察触发条件并列出支持/反证，可能减少无根据的架构升级和用户二次纠正。
- 更窄的场景复核可能降低修复目标漂移，但不能因此漏掉用户明确新增的架构目标。

一例不能推出系统普遍误解率，也不应把相关性写成根因。

## 验收场景

1. 准备 8 个固定的“用户纠正故障场景”配对任务，冻结模型、工具和初始状态；用户第一次描述后再给出明确纠正。
2. 盲评 Agent 是否先保留原始故障的触发条件、区分观察/推断/假设，并以证据排除解释，而非直接升级成新架构目标。
3. 评估同时覆盖明确要求新架构的反例：用户真的新增目标时，Agent 必须保留该目标，不得把它压回局部修复。
4. 不增加不必要询问，不削弱原有授权边界；记录首次有效场景、修复轮次和遗漏的真实新增目标。

## 范围边界

- 范围限于任务 prompt、纠正后的场景复核流程和可重复的行为评估；不直接把一例历史对话改写成产品架构结论。
- 不在本 issue 中决定 cron、heartbeat 或统一调度架构，不修改实际调度实现；若复核证明存在实现缺陷，应另建实现 issue。
- 不把已有“verifiable goal”前缀计数视为 provider 输入完整性或因果证据。

## 复现条件

- **复现频率**：当前只在一个抽样 focal task 观察到，需配对实验确认。
- **触发步骤**：
  1. 提出一个带具体机制/触发时机的故障描述。
  2. 让 Agent 提出解释或修复方向。
  3. 用户明确纠正为更窄场景，或明确新增更大目标。
  4. 检查 Agent 是否重新锁定场景并保持授权与目标边界。
- **环境**：ACP doing-tasks prompt、Ultra Task 指导和固定 provider/tool 配置。

## 涉及文件

- `peri-acp/prompts/sections/03_doing_tasks.md` —— 任务理解、证据与运行时事实提示。
- `peri-middlewares/src/skills/builtin/skills/ultra-task/SKILL.md` —— 任务边界、验收和证据收敛指导。

## 状态变更记录

| 日期 | 从 | 到 | 操作人 | 说明 |
|------|-----|-----|--------|------|
| 2026-09-13 | — | Open | agent | 依据本轮用户授权和观察队列创建；待实际执行验证 |

## 修复记录

（由 auto-issue-fixer 修复阶段追加，创建时留空）
