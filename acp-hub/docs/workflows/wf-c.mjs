export const meta = {
  name: 'acp-hub-wf-c',
  description: 'acp-hub M1 阶段C：F5 server 协议/通道/控制面 + F6 machine（各 feature 内部 plan→code→review+test，显式 sonnet 模型）'
}

const COMMON = [
  '背景：/Users/konghayao/code/ai/perihelion/acp-hub 是独立 workspace（members: proto/server/machine）。',
  '已完成：proto crate（帧模型/Action 信封/machine 协议/Y.Doc schema 镜像/M1 帧集白名单/HMAC 原语）；',
  'server 的 config/（Config 加载链、allow_peer、is_env_key_allowed、ensure_dirs）、auth/（TokenStore、TokenRole、HMAC 握手、nonce）、',
  'persist/（update 日志、outbox 状态机、水位、compact）、state/（NormalizedEvent、DocManager 单写者微批次、Aggregator 幂等投影、Permission CAS、Registry）；',
  'server 140 tests 全绿、clippy 零警告。machine/ 有旧单机版代码（child.rs 等）与占位模块（transport.rs/buffer.rs/auth.rs）。',
  '架构权威文档：docs/architecture.md。',
  '',
  '协作纪律（必须遵守）：',
  '1. 你只操作自己负责的模块文件（见任务）；严禁修改 lib.rs、Cargo.toml（依赖已预填，如需新增依赖记录到输出由主管处理）、其他 feature 的模块文件。',
  '2. 若需把占位单文件扩展为目录（如 protocol.rs → protocol/mod.rs），可 git rm 原单文件后建目录，但只限自己的模块。',
  '3. 每个公共项写 doc 注释；不修改 docs/architecture.md 与 docs/plans/ 其他文件。',
  '4. 日志用 tracing，遵守 §9.3 脱敏（不记正文/工具参数/token/密钥）。',
  '5. 若设计文档与架构文档冲突，以架构文档为准；无法裁决的冲突记录到输出。',
  ''
].join('\n')

phase('F5/F6 并行')

const [f5, f6] = await parallel([
  () => (async () => {
    const plan = await agent(`你是 acp-hub 架构规划 agent，负责 Feature F5：server 协议/通道/控制面设计（本工程最核心的装配层）。\n\n${COMMON}\n\n任务：阅读 docs/architecture.md 以下章节（Read 分段）：§3（角色与链路：3.1 三层拓扑、3.3 连接建立时序、3.4 断链语义、3.5 心跳与关闭码）、§4.1/4.2/4.3/4.4（帧、Action envelope、Ack/错误码、commandId 去重与 outbox 交互、delivery 三级）、§4.5/4.5.1/4.6/4.7/4.8（machine 协议、epoch、连接重连与补推、keep_alive、M1 帧集白名单）、§6.1/6.2（ACPChannel 规范化边界与事件映射表、连接建立时序与提交点）、§7.1（机器离线与 agent 不可用裁决）、§7.2/7.3（Turn 状态机、session 生命周期）、§7.4（并发规则）、§8.3/8.5（微批次广播、last_seq 补推与 gap）、§9.2（双向认证与连接上下文）、§10（审计）、§16（默认值）、§17（degraded）。\n再快速浏览已有 API（grep pub fn）：server/src/config/mod.rs、server/src/auth/mod.rs、server/src/persist/mod.rs、server/src/state/doc_manager.rs、server/src/state/normalized.rs、server/src/state/registry.rs、server/src/main.rs（F2 已实现的 CLI/token 命令与 run_with 骨架）、proto/src/（conn.rs 帧类型、action.rs、machine.rs、whitelist.rs、hmac.rs）。\n\n产出设计文档（Write 到 docs/plans/f5-channel-control.md），必须覆盖：\n1. 模块划分：protocol/（acp_channel.rs 入站规范化 + translator.rs 出站翻译）、channel/（gateway.rs/session_channel.rs/command_coordinator.rs/relay_event_handler.rs/broadcaster.rs/connection_registry.rs）、control/（machine_registry.rs/session_registry.rs/coordinator 装配/心跳与关闭码）——给出每个模块的核心类型与公开函数签名\n2. ACPChannel：双格式 sessionId 提取（原始 {type,payload} 与 JSON-RPC 包裹）、事件映射表（agent_message_chunk→message_delta、agent_reasoning_chunk→reasoning_delta、tool_call_started/updated/completed、permission_requested/resolved/expired、agent status 等，以 §6.1 表为准）、未知帧 → UNSUPPORTED_FRAME\n3. Translator：出站 action → ACP JSON-RPC（cwd/rpcId 注入、方法面映射）\n4. Gateway：ws accept（tokio-tungstenite）、连接级认证（machine token 双向 HMAC / client token 校验）、快照时序（§6.2：client 连接 → 读 yjs 快照 → ready）、keep_alive/pong（§4.7：5s 周期、4501 关闭）、关闭码 4500/4502、非回环拒绝（config.allow_peer）\n5. CommandCoordinator：每 session 串行队列（64 上限）、commandId 去重（与 outbox 去重索引协作）、入队临界区（session 状态 + 队列满同一临界区）、Action/Ack 两阶段确认（accepted 立即、committed 投影后）\n6. RelayEventHandler：machine 入站事件消费（校验 epoch/seq、写 outbox 意图记录、投 DocManager）、断链时清理（session 状态迁移、权限批量 expired、gap 标记）\n7. Broadcaster：fan-out（每连接每 doc 订阅）、背压（64KB/128KB 两级）、merge_updates、慢连接断开\n8. ConnectionRegistry：配额 200、连接上下文（ConnectionCtx：token_id/role/binding）\n9. MachineRegistry：注册表（REGISTERED/ONLINE/OFFLINE）、心跳 30s 判定离线、hello 幂等 fencing（同 machine 重连踢旧连接）、spawn/kill 指令下发与 ack 跟踪\n10. 会话/回合状态机：accepting/ended/closed/crashed/gap/pending_close；accepting/running/awaiting_permission/cancelling/completed/failed/cancelled/interrupted；绑定（session/new → binding → committed）；pending_close 补发\n11. 断链与恢复：machine 离线 → 活 session 置 interrupted + gap；buffer_sync 补推协调（epoch 校验、from_seq=last_seq+1、排空后恢复实时）；恢复对账（alive_sessions 比对）\n12. 提交点纪律：outbox 落盘 → 下发 → L1/L2 → 投影 → committed Ack（§4.4/§6.2 时序图）\n13. 服务装配：run_with 扩展（监听 127.0.0.1:8456 默认、优雅关闭、Degraded 判定入口）、孤儿进程清理钩子（§7.5，实际清理在 machine 侧 kill）\n14. 测试清单（每个模块的单元测试 + 需要的 fake machine ws 客户端）\n\n输出：设计文档路径 + 5 条左右关键决策摘要。`, { label: 'f5-plan', model: 'sonnet', allowedTools: ['Read','Write','Glob','Grep'] })

    const code = await agent(`你是 acp-hub 实现 agent，负责 Feature F5：实现 server 协议/通道/控制面。\n\n${COMMON}\n\n任务：\n1. 先 Read docs/plans/f5-channel-control.md（必须遵守其设计），按需核对 architecture.md 对应章节与已有模块 API（config/auth/persist/state、proto 帧类型）。\n2. 实现 server/src/protocol/、server/src/channel/、server/src/control/ 三个模块（占位单文件可转为目录）。保持与 state 层（NormalizedEvent/DocManager）、persist 层（outbox 状态机）、auth 层（TokenStore/ConnectionCtx）的接口对齐；不修改这些已有模块。\n3. 扩展 server/src/main.rs 的 run_with：启动监听（127.0.0.1:8456 默认，config.allow_peer 校验）、装配全部组件、优雅关闭（SIGINT/SIGTERM）。main.rs 已有 CLI/token 命令骨架，保持兼容。\n4. 写测试：\n   - ACPChannel 双格式 sessionId 提取 + 事件映射表逐条 + 未知帧 UNSUPPORTED_FRAME\n   - Translator 出站映射\n   - CommandCoordinator 去重（同 commandId 二次提交 → duplicate）+ 队列满\n   - Gateway 时序（fake ws client：连接 → auth → ready → 快照；keep_alive 超时 4501）\n   - MachineRegistry 心跳离线判定（tokio::time::pause 或短超时配置）、hello fencing\n   - 断链语义（machine 离线 → session interrupted + 权限 expired + gap）\n   - 补推协调（epoch 校验、from_seq 计算）\n   - 端到端单测：fake machine ws 客户端 → spawn → 事件流 → 客户端收到广播\n5. 验证：cd /Users/konghayao/code/ai/perihelion/acp-hub && cargo test -p acp-hub-server && cargo clippy -p acp-hub-server --all-targets -- -D warnings。\n\n输出：模块文件清单、核心类型/函数签名摘要、测试结果（测试名列表）、clippy 结果、遗留问题（含需补的依赖）。`, { label: 'f5-code', model: 'sonnet', allowedTools: ['Read','Write','Edit','Bash','Glob','Grep','folder_operations'] })

    const review = await agent(`你是 acp-hub 代码审查 agent，负责审查 Feature F5（server 协议/通道/控制面）。\n\n${COMMON}\n\n任务：\n1. 对照 architecture.md §3/§4/§6/§7/§8.3/§8.5/§9.2/§16/§17 与 docs/plans/f5-channel-control.md，审查 server/src/protocol/、channel/、control/ 与 main.rs 装配：\n   - 事件映射表逐条核对 §6.1（映射遗漏/错误映射）、双格式 sessionId 提取\n   - 提交点纪律（outbox 落盘 → 下发 → L1/L2 → 投影 → committed Ack 顺序）\n   - 去重语义（commandId 去重与 outbox 索引一致）、队列临界区\n   - gateway 认证与快照时序（§6.2 顺序）、keep_alive/关闭码（§4.7 4500/4501/4502）\n   - 断链语义（machine 离线 → interrupted + 权限 expired + gap + pending_close）、epoch 校验、补推 from_seq\n   - 背压（64KB/128KB）、连接配额、degraded 判定入口\n   - 安全：token 不落日志、非回环拒绝、审计\n2. 运行：cd /Users/konghayao/code/ai/perihelion/acp-hub && cargo test -p acp-hub-server && cargo clippy -p acp-hub-server --all-targets -- -D warnings。失败/警告直接修复（只改 protocol/channel/control/main.rs）。\n3. 输出：结论（通过/不通过）、问题清单按严重级（P0/P1/P2）、已修复项、未修复项及理由。`, { label: 'f5-review', model: 'sonnet', allowedTools: ['Read','Write','Edit','Bash','Glob','Grep','folder_operations'] })

    return { plan, code, review }
  })(),

  () => (async () => {
    const plan = await agent(`你是 acp-hub 架构规划 agent，负责 Feature F6：acp-machine 设计（outbound 连 server、ACP 进程管理、断线缓冲）。\n\n${COMMON}\n\n任务：阅读 docs/architecture.md：§3.1（三层拓扑与 machine 职责）、§4.5/4.5.1（machine 协议全表、stream_epoch、缓冲分桶规则）、§4.6（重连补推）、§7.1（机器离线与超时）、§7.5（孤儿进程 kill）、§8.3（machine 缓冲：每 session 分桶、delta 优先、10MB/万条上限、单帧 1MB）、§8.5（环形滑窗 500 条、buffer_sync 排空后恢复实时）、§9.2（machine 侧 HMAC 校验 server 身份）、§9.6（env 白名单）、§16（默认值表）。再读现有代码：machine/src/child.rs（现有 ACP 子进程管理，迁移基线）、machine/src/main.rs、machine/src/hub.rs、machine/src/router.rs、machine/src/global.rs（旧单机版，决定哪些保留哪些废弃）、proto/src/machine.rs 与 conn.rs（machine 帧类型）、proto/src/hmac.rs（HMAC 原语）。\n\n产出设计文档（Write 到 docs/plans/f6-machine.md），覆盖：\n1. 模块划分：child.rs（保留改造：spawn/kill/监控/转发，kill_on_drop、进程组 kill）、transport.rs（outbound ws、单连接多路复用、指数退避重连 1s→60s、心跳 5s）、buffer.rs（按 session 分桶、内存+磁盘两级（10MB/万条）、delta 优先丢弃、单帧 1MB 截断告警）、auth.rs（machine 侧双向认证：hello 带 nonce、校验 server HMAC 应答、重连时重新握手）\n2. 连接生命周期：启动 → 读配置（server 地址/token/machine id）→ ws 连接 → hello → 双向认证 → 就绪；断连 → 缓冲 → 退避重连 → 重新认证 → buffer_sync 补推（epoch 校验、from_seq=last_seq+1、滑窗 500）\n3. seq 与 epoch 分配：每 session 单调 seq（spawn 时从 0 开始？以架构为准）、stream_epoch 生成（重启时新 epoch）、last_seq 持久化（磁盘水位文件，跨重启）\n4. spawn/kill 幂等执行（按 session_id 幂等、已存在返回现有句柄）、spawn_ack/kill_ack 上报、process_exit 上报（含退出码）\n5. 缓冲实现：内存 VecDeque + 磁盘 append 日志（复用 proto 帧序列化）、重连后 buffer_sync 协议细节、缓冲溢出丢弃策略与告警（§17.2 degraded 信号）\n6. 孤儿进程清理：kill_on_drop、进程组 kill（setsid）、启动时清理残留（§7.5）\n7. 与旧代码的关系：child.rs 迁移改造清单；hub.rs/router.rs/global.rs 的废弃或保留决策（旧单机 stdio 桥接逻辑不再需要，确认后删除或标注 deprecated）\n8. 测试清单（重连退避、缓冲分桶与丢弃、seq 单调、spawn 幂等、kill 幂等、process_exit、buffer_sync 补推）\n\n输出：设计文档路径 + 5 条左右关键决策摘要。`, { label: 'f6-plan', model: 'sonnet', allowedTools: ['Read','Write','Glob','Grep'] })

    const code = await agent(`你是 acp-hub 实现 agent，负责 Feature F6：实现 acp-machine。\n\n${COMMON}\n\n任务：\n1. 先 Read docs/plans/f6-machine.md（必须遵守其设计），按需核对 architecture.md 对应章节与 proto 帧类型。\n2. 改造 machine/src/child.rs：保留 ACP 子进程 spawn/通信/kill 能力，补齐 kill_on_drop、进程组 kill（setsid + kill(-pgid)）、按 session_id 幂等句柄管理。\n3. 实现 machine/src/transport.rs：tokio-tungstenite outbound ws、单连接、指数退避重连（1s 起 60s 上限，抖动）、心跳 5s、帧读写（proto 帧序列化）。\n4. 实现 machine/src/buffer.rs：每 session 分桶（内存 + 磁盘水位文件）、delta 优先丢弃、单帧上限、buffer_sync 补推逻辑（epoch 校验、from_seq=last_seq+1、滑窗 500）。\n5. 实现 machine/src/auth.rs：hello 握手（携带 machine token、challenge nonce）、校验 server HMAC 应答（复用 proto hmac 原语）、重连重新握手。\n6. 装配 machine/src/main.rs：读配置（命令行/env/默认，server 地址、machine token、machine id）、启动连接循环、接入 child 事件流。旧 hub.rs/router.rs/global.rs 按设计文档决策处理（删除或 deprecated）。\n7. 写测试：child spawn/kill 幂等与进程组 kill、重连退避（短超时配置）、缓冲分桶与丢弃、seq 单调与 epoch 变化、buffer_sync 补推（fake server ws）、auth 握手成功/失败。\n8. 验证：cd /Users/konghayao/code/ai/perihelion/acp-hub && cargo test -p acp-machine && cargo clippy -p acp-machine --all-targets -- -D warnings。\n\n输出：模块文件清单、核心类型/函数签名摘要、测试结果（测试名列表）、clippy 结果、遗留问题。`, { label: 'f6-code', model: 'sonnet', allowedTools: ['Read','Write','Edit','Bash','Glob','Grep','folder_operations'] })

    const review = await agent(`你是 acp-hub 代码审查 agent，负责审查 Feature F6（acp-machine）。\n\n${COMMON}\n\n任务：\n1. 对照 architecture.md §3.1/§4.5/§4.5.1/§4.6/§7.1/§7.5/§8.3/§8.5/§9.2/§9.6 与 docs/plans/f6-machine.md，审查 machine/src/ 全部实现：\n   - child：进程组 kill、kill_on_drop、幂等句柄、env 白名单（config.is_env_key_allowed）\n   - transport：重连退避上限与抖动、心跳超时处理、帧读写错误处理\n   - buffer：分桶正确性、丢弃策略（delta 优先）、单帧上限、磁盘水位、epoch/seq 单调性\n   - auth：握手流程（hello → 校验 HMAC 应答）、token 不落日志\n   - 与 proto 帧类型一致（serde tag、字段名）\n2. 运行：cd /Users/konghayao/code/ai/perihelion/acp-hub && cargo test -p acp-machine && cargo clippy -p acp-machine --all-targets -- -D warnings。失败/警告直接修复（只改 machine/ 与 machine 测试）。\n3. 输出：结论（通过/不通过）、问题清单按严重级、已修复项、未修复项及理由。`, { label: 'f6-review', model: 'sonnet', allowedTools: ['Read','Write','Edit','Bash','Glob','Grep','folder_operations'] })

    return { plan, code, review }
  })(),
])

return { f5, f6 }
