# Rewind v2（历史驱动回溯）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **项目约定（CLAUDE.md）**：本仓库**仅在用户明确要求时执行 commit**。计划中每个任务的 commit 步骤为可选操作——若用户未要求，跳过该步继续；若要求，按步骤执行。

**Goal:** 将 rewind 改为"TUI 本地历史驱动 + 打开面板实时查询 + 文件回退预算先行 + 目标文本回填输入框"的全新流程，替换当前"服务端每轮推送 preview"的实现。

**Architecture:** 服务端新增 3 个同步 RPC（`session/rewind-candidates` 查询 user 消息候选、`session/rewind-preview` 计算文件回退预算、`session/rewind` 执行回退），注册在 `peri-tui/src/acp_server/requests.rs`；TUI 双击 Esc 时打开弹窗并异步查询候选，Enter 后先取预算（空则自动执行），执行完成经 `RewindCompleted` 事件刷新消息区并回填输入框。服务端 `RewindCommand` 的执行逻辑与文件复原（最佳努力）保持不变，仅将提取函数提升为 `pub(crate)` 供预算复用。上一轮修复加入的 `executor_helpers.rs` Phase 8.7 推送整体退役。

**Tech Stack:** Rust, tokio, serde_json, ratatui-kit（atoms/popup 组件）, JSON-RPC（mpsc transport）

**Spec:** `docs/superpowers/specs/2026-08-01-rewind-v2-design.md`

---

## 文件结构总览

**服务端（peri-acp）**
- `peri-acp/src/session/command/rewind.rs` — 修改：`FileChange` / `extract_file_changes` / `parse_tool_call` 提升 `pub(crate)`
- `peri-acp/src/dispatch/rewind_candidates.rs` — 新建：候选查询（纯提取）
- `peri-acp/src/dispatch/rewind.rs` — 新建：预算（只读）+ 执行（复用 RewindCommand）
- `peri-acp/src/dispatch/mod.rs` — 修改：注册新模块
- `peri-acp/src/session/executor_helpers.rs` — 修改：删除 Phase 8.7 推送 + `build_rewind_preview_payload`
- `peri-acp/src/session/executor.rs` — 修改：移除 re-export
- `peri-acp/src/session/executor_test.rs` — 修改：删除 payload 测试

**TUI（peri-tui）**
- `peri-tui/src/acp_server/requests.rs` — 修改：注册 3 个 RPC 分支
- `peri-tui/src/kit/atoms.rs` — 修改：新增 `REWIND_TARGET_TEXT` / `REWIND_BUDGET_STATE` / `REWIND_QUERY_ERROR`
- `peri-tui/src/kit/rewind_candidates.rs` — 新建：候选查询发送 + 响应解析
- `peri-tui/src/kit/rewind_action.rs` — 修改：`RewindAction` 两段式（Preview/Confirm）+ consumer
- `peri-tui/src/kit/popups/rewind_popup.rs` — 重写：候选列表（只 user）+ 预算确认视图 + 执行中态
- `peri-tui/src/kit/event_handlers.rs` — 修改：双击 Esc → open_popup + spawn 候选查询
- `peri-tui/src/kit/acp_events/system.rs` — 修改：`handle_rewind_preview` 退役；`handle_rewind_completed` 回填输入框
- `peri-tui/src/kit/acp_events.rs`（dispatch match）— 修改：移除 `RewindPreview` arm
- `peri-tui/src/kit/popup_overlay.rs` — 修改：close_popup(Rewind) 清预算/目标文本/查询错误，保留候选
- `peri-tui/src/kit/submit_consumer.rs` / `thread_load_consumer.rs` — 修改：会话边界清理新 atoms

**测试文件**
- `peri-acp/src/dispatch/rewind_candidates_test.rs`（新建）/ `rewind_test.rs`（新建）
- `peri-acp/src/session/command/rewind_test.rs`（不变，回归）
- `peri-tui/src/kit/rewind_candidates_test.rs`（新建）/ `rewind_action_test.rs` / `rewind_popup_test.rs` / `acp_events_test.rs` / `popup_overlay_test.rs`（更新）

---

## Task 1: 服务端 — 提取函数 pub(crate) 化

**Files:**
- Modify: `peri-acp/src/session/command/rewind.rs:37-46,158-224`

将预算阶段复用的三个项提升为 `pub(crate)`（当前为模块私有 `enum FileChange` / `fn extract_file_changes` / `fn parse_tool_call`）。

- [x] **Step 1: 修改可见性**

```rust
// rewind.rs 顶部（enum 定义处，37-46 行）
pub(crate) enum FileChange {
    Write {
        path: String,
    },
    Edit {
        path: String,
        old_string: String,
        new_string: String,
    },
}

// 158 行
pub(crate) fn extract_file_changes(messages: &[BaseMessage]) -> Vec<FileChange> {

// 198 行
pub(crate) fn parse_tool_call(name: &str, args: &serde_json::Value) -> Option<FileChange> {
```

- [x] **Step 2: 编译检查**

Run: `cargo check -p peri-acp`
Expected: PASS（现有 `rewind_test.rs` 用 `super::` 引用，`pub(crate)` 不破坏其可见性）

- [x] **Step 3: 回归测试**

Run: `cargo test -p peri-acp --lib rewind`
Expected: PASS（现有 rewind 测试全部通过）

- [x] **Step 4: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-acp/src/session/command/rewind.rs
git commit -m "refactor(peri-acp): expose rewind extract_file_changes as pub(crate) for budget reuse"
```

---

## Task 2: 服务端 — dispatch/rewind_candidates.rs

**Files:**
- Create: `peri-acp/src/dispatch/rewind_candidates.rs`
- Test: `peri-acp/src/dispatch/rewind_candidates_test.rs`
- Modify: `peri-acp/src/dispatch/mod.rs`

候选查询：从 session history 提取 user 消息（`BaseMessage::Human` 且文本不含 `<system-reminder>`），返回 `{ messages: [{id, preview}] }`。

- [x] **Step 1: 写失败测试**

```rust
//! dispatch/rewind_candidates 单元测试。

use peri_agent::messages::BaseMessage;
use serde_json::json;

use super::rewind_candidates;

#[test]
fn test_candidates_extracts_only_user_messages() {
    let history = vec![
        BaseMessage::human("第一轮用户问题"),
        BaseMessage::ai("第一轮回答"),
        BaseMessage::human("第二轮用户问题"),
    ];

    let result = rewind_candidates(&history).unwrap();
    let messages = result["messages"].as_array().unwrap();

    assert_eq!(messages.len(), 2, "只提取 user 消息");
    assert_eq!(messages[0]["preview"], "第一轮用户问题");
    assert_eq!(messages[1]["preview"], "第二轮用户问题");
    assert!(
        messages[0]["id"].as_str().unwrap().len() >= 8,
        "携带服务端权威消息 id"
    );
}

#[test]
fn test_candidates_excludes_system_reminder_injection() {
    let history = vec![
        BaseMessage::human("正常用户输入"),
        BaseMessage::human("<system-reminder>后台任务完成通知</system-reminder>"),
    ];

    let result = rewind_candidates(&history).unwrap();
    let messages = result["messages"].as_array().unwrap();

    assert_eq!(messages.len(), 1, "系统注入消息不进入候选");
    assert_eq!(messages[0]["preview"], "正常用户输入");
}

#[test]
fn test_candidates_empty_history_returns_empty_list() {
    let result = rewind_candidates(&[]).unwrap();
    assert_eq!(result["messages"].as_array().unwrap().len(), 0);
}

#[test]
fn test_candidates_preview_truncated_to_200_chars() {
    let long = "x".repeat(500);
    let history = vec![BaseMessage::human(&long)];

    let result = rewind_candidates(&history).unwrap();
    let preview = result["messages"][0]["preview"].as_str().unwrap();
    assert_eq!(preview.chars().count(), 200);
}
```

- [x] **Step 2: 跑测试确认失败**

Run: `cargo test -p peri-acp --lib rewind_candidates`
Expected: FAIL（模块/函数不存在，编译错误）

- [x] **Step 3: 实现**

```rust
//! `session/rewind-candidates` dispatch handler.
//!
//! 查询回退候选：从 session history 提取 user 消息（`BaseMessage::Human`），
//! 排除文本包含 `<system-reminder>` 的系统注入消息（与 TUI `ReminderInfo`
//! 检测口径一致）。返回 `{ messages: [{ id, preview }] }`，id 为服务端权威
//! `MessageId`，preview 截断 200 字符。

use peri_agent::messages::BaseMessage;
use serde_json::{Value, json};

use crate::transport::types::AcpError;

/// 提取回退候选（纯计算，无副作用）。
pub fn rewind_candidates(session_history: &[BaseMessage]) -> Result<Value, AcpError> {
    let messages: Vec<Value> = session_history
        .iter()
        .filter(|m| matches!(m, BaseMessage::Human { .. }))
        .filter(|m| !m.content().contains("<system-reminder>"))
        .map(|m| {
            json!({
                "id": m.id().as_uuid().to_string(),
                "preview": m.content().chars().take(200).collect::<String>(),
            })
        })
        .collect();

    Ok(json!({ "messages": messages }))
}

#[cfg(test)]
#[path = "rewind_candidates_test.rs"]
mod tests;
```

- [x] **Step 4: 注册模块**

在 `peri-acp/src/dispatch/mod.rs` 的 `pub mod` 列表中加入（按字母序，`prompt` 之后）：

```rust
pub mod rewind;
pub mod rewind_candidates;
```

并在 `pub use` 区加入：

```rust
pub use rewind::{rewind_execute, rewind_preview};
pub use rewind_candidates::rewind_candidates;
```

> 注：`pub use` 语句在 Task 3 完成后才能编译通过——若本步报 unresolved import，先注释掉 `pub use rewind::{...}` 行，Task 3 完成后恢复。

- [x] **Step 5: 跑测试确认通过**

Run: `cargo test -p peri-acp --lib rewind_candidates`
Expected: PASS（4 个测试）

- [x] **Step 6: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-acp/src/dispatch/rewind_candidates.rs peri-acp/src/dispatch/rewind_candidates_test.rs peri-acp/src/dispatch/mod.rs
git commit -m "feat(peri-acp): add session/rewind-candidates dispatch (user-only candidates)"
```

---

## Task 3: 服务端 — dispatch/rewind.rs（预算 + 执行）

**Files:**
- Create: `peri-acp/src/dispatch/rewind.rs`
- Test: `peri-acp/src/dispatch/rewind_test.rs`

预算（只读提取 Write/Edit）+ 执行（复用 `RewindCommand`）。

- [x] **Step 1: 写失败测试**

参考 `peri-acp/src/session/command/rewind_test.rs` 的 MockEventSink 基建（`events: Mutex<Vec<(String, String)>>` + `push_done_count`），测试预算提取与执行：

```rust
//! dispatch/rewind 单元测试（预算 + 执行）。

use std::sync::{Arc, Mutex};

use async_trait::async_trait;
use peri_agent::{
    agent::events::ExecutorEvent,
    messages::{BaseMessage, ContentBlock, ToolCallRequest},
};

use super::{rewind_preview, RewindArgs};
use crate::session::event_sink::EventSink;

// ── Mock EventSink（与 command/rewind_test.rs 同构）──

struct MockEventSink {
    events: Mutex<Vec<(String, String)>>,
}

impl MockEventSink {
    fn new() -> Self {
        Self {
            events: Mutex::new(Vec::new()),
        }
    }
}

#[async_trait]
impl EventSink for MockEventSink {
    async fn push_event(&self, session_id: &str, event: &ExecutorEvent, _context_window: u32) {
        let json = serde_json::to_string(event).unwrap_or_default();
        self.events
            .lock()
            .unwrap()
            .push((session_id.to_string(), json));
    }

    async fn push_done(&self, _session_id: &str, _stop_reason: &str) {}
}

/// 构造带工具调用的历史：U1 → A1(Edit) → U2 → A2(Write)
fn make_history_with_tools() -> Vec<BaseMessage> {
    vec![
        BaseMessage::human("第一轮问题"),
        BaseMessage::ai_with_tool_calls(
            "编辑文件",
            vec![ToolCallRequest {
                id: "tc-edit".into(),
                name: "Edit".into(),
                arguments: serde_json::json!({
                    "file_path": "src/main.rs",
                    "old_string": "old",
                    "new_string": "new",
                }),
            }],
        ),
        BaseMessage::human("第二轮问题"),
        BaseMessage::ai_with_tool_calls(
            "写文件",
            vec![ToolCallRequest {
                id: "tc-write".into(),
                name: "Write".into(),
                arguments: serde_json::json!({
                    "file_path": "new_file.txt",
                }),
            }],
        ),
    ]
}

#[tokio::test]
async fn test_preview_lists_file_changes_after_target() {
    let history = make_history_with_tools();
    let sink: Arc<dyn EventSink> = Arc::new(MockEventSink::new());
    let target_id = history[2].id().as_uuid().to_string(); // U2

    let result = rewind_preview(
        &serde_json::json!({ "target_message_id": target_id }),
        &history,
        &sink,
        "test-session",
    )
    .await
    .unwrap();

    let changes = result["file_changes"].as_array().unwrap();
    assert_eq!(changes.len(), 1, "目标之后只有 Write");
    assert_eq!(changes[0]["path"], "new_file.txt");
    assert_eq!(changes[0]["kind"], "write");
}

#[tokio::test]
async fn test_preview_reverse_order_newest_first() {
    let history = make_history_with_tools();
    let sink: Arc<dyn EventSink> = Arc::new(MockEventSink::new());
    let target_id = history[0].id().as_uuid().to_string(); // U1

    let result = rewind_preview(
        &serde_json::json!({ "target_message_id": target_id }),
        &history,
        &sink,
        "test-session",
    )
    .await
    .unwrap();

    let changes = result["file_changes"].as_array().unwrap();
    assert_eq!(changes.len(), 2);
    assert_eq!(changes[0]["path"], "new_file.txt", "逆序：最新变更在前");
    assert_eq!(changes[1]["path"], "src/main.rs");
    assert_eq!(changes[1]["kind"], "edit");
}

#[tokio::test]
async fn test_preview_target_not_found_returns_error() {
    let history = make_history_with_tools();
    let sink: Arc<dyn EventSink> = Arc::new(MockEventSink::new());

    let result = rewind_preview(
        &serde_json::json!({ "target_message_id": "nonexistent" }),
        &history,
        &sink,
        "test-session",
    )
    .await;

    assert!(result.is_err(), "目标不存在应返回错误");
}

#[tokio::test]
async fn test_preview_no_file_changes_returns_empty_list() {
    let history = vec![
        BaseMessage::human("你好"),
        BaseMessage::ai("你好！有什么可以帮你？"),
    ];
    let sink: Arc<dyn EventSink> = Arc::new(MockEventSink::new());
    let target_id = history[0].id().as_uuid().to_string();

    let result = rewind_preview(
        &serde_json::json!({ "target_message_id": target_id }),
        &history,
        &sink,
        "test-session",
    )
    .await
    .unwrap();

    assert_eq!(
        result["file_changes"].as_array().unwrap().len(),
        0,
        "无文件改动 → 空预算列表"
    );
}

/// Anthropic ContentBlock::ToolUse 格式也需提取（与 RewindCommand 同规则）。
#[tokio::test]
async fn test_preview_extracts_anthropic_tool_use() {
    let history = vec![
        BaseMessage::human("改一下"),
        BaseMessage::ai_with_content(
            "改文件",
            vec![ContentBlock::ToolUse {
                id: "block-1".into(),
                name: "Edit".into(),
                input: serde_json::json!({
                    "file_path": "docs/readme.md",
                    "old_string": "a",
                    "new_string": "b",
                }),
            }],
        ),
    ];
    let sink: Arc<dyn EventSink> = Arc::new(MockEventSink::new());
    let target_id = history[0].id().as_uuid().to_string();

    let result = rewind_preview(
        &serde_json::json!({ "target_message_id": target_id }),
        &history,
        &sink,
        "test-session",
    )
    .await
    .unwrap();

    let changes = result["file_changes"].as_array().unwrap();
    assert_eq!(changes.len(), 1);
    assert_eq!(changes[0]["path"], "docs/readme.md");
}
```

> 注：`BaseMessage::ai_with_tool_calls` / `ai_with_content` 若不存在，参考 `peri-agent/src/messages/message.rs` 的构造器名称（`BaseMessage::ai` + 手动构造 `tool_calls` 字段，或 `ContentBlock` 直接构造 `BaseMessage::Ai { .. }` 结构体字面量——以 `rewind_test.rs` 现有写法为准）。

- [x] **Step 2: 跑测试确认失败**

Run: `cargo test -p peri-acp --lib dispatch::rewind`
Expected: FAIL（模块不存在）

- [x] **Step 3: 实现**

```rust
//! `session/rewind-preview` 与 `session/rewind` dispatch handlers。
//!
//! - `rewind_preview`：只读计算文件回退预算——定位目标消息，提取目标之后
//!   （含目标）被移除消息中的 Write/Edit 工具调用，按时间逆序返回。
//! - `rewind_execute`：执行回退——复用 `RewindCommand`（截断 + 文件复原 +
//!   配对校验 + 持久化删除 + RewindCompleted 事件）。

use std::sync::Arc;

use serde_json::{Value, json};
use tracing::warn;

use crate::{
    provider::PeriConfig,
    session::{
        command::{CommandContext, RewindCommand, extract_file_changes},
        event_sink::EventSink,
        executor::PromptStopReason,
    },
    transport::types::AcpError,
};

/// 解析 `session/rewind` 系请求的公共参数。
#[derive(serde::Deserialize)]
pub struct RewindArgs {
    pub target_message_id: String,
}

/// 计算文件回退预算（只读，不修改任何状态）。
///
/// 返回 `{ "file_changes": [{ "path", "kind" }] }`，kind ∈ {"write", "edit"}，
/// 按时间逆序（最新变更在前）。
pub async fn rewind_preview(
    params: &Value,
    session_history: &[peri_agent::messages::BaseMessage],
    event_sink: &Arc<dyn EventSink>,
    session_id: &str,
) -> Result<Value, AcpError> {
    let args: RewindArgs = serde_json::from_value(params.clone())
        .map_err(|e| AcpError::new(-32602, format!("rewind-preview 参数解析失败: {e}")))?;

    let target_idx = session_history
        .iter()
        .position(|m| m.id().as_uuid().to_string() == args.target_message_id);

    let target_idx = match target_idx {
        Some(i) => i,
        None => {
            let msg = format!("rewind: 未找到目标消息 {}", args.target_message_id);
            warn!(msg);
            event_sink
                .push_event(
                    session_id,
                    &peri_agent::agent::events::ExecutorEvent::CompactError {
                        message: msg.clone(),
                    },
                    0,
                )
                .await;
            return Err(AcpError::new(-32602, msg));
        }
    };

    let removed_messages = &session_history[target_idx..];
    let mut changes: Vec<Value> = extract_file_changes(removed_messages)
        .iter()
        .rev() // 逆序：最新变更在前
        .map(|fc| {
            let kind = match fc {
                crate::session::command::FileChange::Write { .. } => "write",
                crate::session::command::FileChange::Edit { .. } => "edit",
            };
            let path = match fc {
                crate::session::command::FileChange::Write { path } => path.clone(),
                crate::session::command::FileChange::Edit { path, .. } => path.clone(),
            };
            json!({ "path": path, "kind": kind })
        })
        .collect();

    // 截断语义与 RewindCommand 一致：removed = history[target_idx..]（含目标本身）。
    // 目标为 user 消息不含工具调用，故 extract_file_changes 结果只覆盖目标之后的
    // assistant 工具调用。空预算返回空列表（TUI 据此直接执行、不展示预算视图）。

    Ok(json!({ "file_changes": changes }))
}

/// 执行回退：复用 `RewindCommand`（Immediate 命令）。
///
/// 参数清单与 `dispatch/execute_command.rs::execute_command` 对齐。
#[allow(clippy::too_many_arguments)]
pub async fn rewind_execute(
    params: &Value,
    session_history: Vec<peri_agent::messages::BaseMessage>,
    cwd: &str,
    peri_config: &Arc<PeriConfig>,
    event_sink: &Arc<dyn EventSink>,
    auxiliary_model: Option<Arc<dyn peri_model::Model>>,
    cancel_token: &peri_agent::agent::AgentCancellationToken,
    thread_store: Option<Arc<dyn peri_agent::thread::ThreadStore>>,
    thread_id: Option<String>,
    bg_event_tx: Option<
        tokio::sync::mpsc::UnboundedSender<peri_agent::agent::events::ExecutorEvent>,
    >,
    bg_registry: Option<Arc<peri_middlewares::subagent::BackgroundTaskRegistry>>,
    frozen_claude_md: Option<Arc<String>>,
    frozen_claude_local_md: Option<Arc<String>>,
    frozen_skill_summary: Option<Arc<String>>,
    frozen_system_prompt: Option<Arc<String>>,
) -> Result<Value, AcpError> {
    let session_id = params
        .get("sessionId")
        .or_else(|| params.get("session_id"))
        .and_then(|v| v.as_str())
        .ok_or_else(|| AcpError::new(-32602, "missing sessionId"))?
        .to_string();

    let ctx = CommandContext {
        session_id: session_id.clone(),
        history: session_history,
        cwd: cwd.to_string(),
        peri_config: Arc::clone(peri_config),
        auxiliary_model,
        event_sink: Arc::clone(event_sink),
        args: params.to_string(),
        cancel_token: cancel_token.clone(),
        thread_store,
        thread_id,
        bg_event_sender: bg_event_tx,
        bg_registry,
        frozen_claude_md,
        frozen_claude_local_md,
        frozen_skill_summary,
        frozen_system_prompt,
    };

    let result = RewindCommand.execute(ctx).await;

    // 与 execute-command dispatch 一致：Immediate 命令绕过 agent event pump，
    // 必须手动 signal completion（TRAP: issue_2026-05-29-immediate-command-missing-push-done）。
    event_sink.push_done(&session_id, "end_turn").await;

    if result.stop_reason == PromptStopReason::Cancelled {
        return Err(AcpError::new(-32603, "rewind cancelled"));
    }

    Ok(json!({ "status": "executed" }))
}

#[cfg(test)]
#[path = "rewind_test.rs"]
mod tests;
```

> 注：`CommandContext` 中 `args` 字段语义是命令参数字符串；`RewindCommand` 从 `ctx.args` 解析 `{ target_message_id }`——`params.to_string()` 序列化的 JSON 对象包含 `sessionId` 与 `target_message_id` 两键，`serde_json::from_str::<RewindArgs>` 只取 `target_message_id`（未知字段忽略），可正常工作。若担心 `sessionId` 干扰，可改为 `json!({"target_message_id": ...}).to_string()`——以 `execute_command` 现有 `args_string` 构造逻辑为准（其将 args 对象整体序列化为字符串，`RewindCommand` 解析不受多余字段影响）。

- [x] **Step 4: 确认 `pub use` 编译通过**

Run: `cargo check -p peri-acp`
Expected: PASS（若 Task 2 Step 4 的 `pub use` 被注释，恢复它）

- [x] **Step 5: 跑测试确认通过**

Run: `cargo test -p peri-acp --lib dispatch::rewind`
Expected: PASS（5 个测试）

- [x] **Step 6: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-acp/src/dispatch/rewind.rs peri-acp/src/dispatch/rewind_test.rs peri-acp/src/dispatch/mod.rs
git commit -m "feat(peri-acp): add session/rewind-preview budget and session/rewind execute dispatch"
```

---

## Task 4: TUI ACP server — 注册 3 个 RPC 分支

**Files:**
- Modify: `peri-tui/src/acp_server/requests.rs`（`handle_request` match 尾部、`session/rename` 之后）
- Test: `peri-tui/src/acp_server/requests_test.rs`

在 `handle_request` 的 match 中加入三个分支。参考 `session/prompt` 分支（`peri-tui/src/acp_server/prompt.rs`）访问 SessionState 与构造 `TransportEventSink` 的模式。

- [x] **Step 1: 读现有代码确认访问模式**

Run: `sed -n '280,375p' peri-tui/src/acp_server/requests.rs`（`session/load` 分支）与 `grep -n "fn handle_request\|sessions" peri-tui/src/acp_server/requests.rs`
Expected: 确认 `sessions: &mut HashMap<String, SessionState>` 在 `handle_request` 参数中、`state.history` 为私有字段（本步同时将 `peri-tui/src/acp_server/mod.rs:43` 的 `history: Vec<BaseMessage>` 改为 `pub(crate) history`）。

- [x] **Step 2: 写失败路由测试（requests_test.rs 追加）**

在 `requests_test.rs` 追加（参考其现有 `handle_request` 调用方式——若测试基建为 `handle_request(&method, &params, &cfg, &mut sessions, &mock_transport)`，沿用；`MockTransport` 实现 `AcpTransport`）：

```rust
#[tokio::test]
async fn test_rewind_candidates_routes_to_dispatch() {
    // Arrange: 注册一个含 user/ai 消息的 session
    let (cfg, mut sessions, transport) = make_test_server();
    let sid = register_session_with_history(&mut sessions, &cfg); // helper：SessionState{ history: vec![human, ai, human] }

    // Act
    let result = handle_request(
        "session/rewind-candidates",
        &serde_json::json!({ "sessionId": sid }),
        &cfg,
        &mut sessions,
        &transport,
    )
    .await;

    // Assert
    let messages = result.unwrap()["messages"].as_array().unwrap();
    assert_eq!(messages.len(), 2, "只返回 user 消息");
}

#[tokio::test]
async fn test_rewind_routes_to_dispatch() {
    // Arrange
    let (cfg, mut sessions, transport) = make_test_server();
    let sid = register_session_with_history(&mut sessions, &cfg);
    let target_id = sessions.get(&sid).unwrap().history[0].id().as_uuid().to_string();

    // Act: 执行（会走 RewindCommand；无 Write/Edit 时仅截断）
    let result = handle_request(
        "session/rewind",
        &serde_json::json!({ "sessionId": sid, "target_message_id": target_id }),
        &cfg,
        &mut sessions,
        &transport,
    )
    .await;

    // Assert
    assert_eq!(result.unwrap()["status"], "executed");
}
```

> 注：若 `requests_test.rs` 现有基建不便构造完整 `SessionState`（字段多），本步测试可降级为"方法名路由不返回 Method not found"的轻量断言（`is_ok()`），完整行为由 dispatch 层单测覆盖。以现有测试基建为准，不强行新增复杂 helper。

- [x] **Step 3: 跑测试确认失败**

Run: `cargo test -p peri-tui --lib requests`
Expected: FAIL（`handle_request` 返回 `Method not found` 错误）

- [x] **Step 4: 实现分支**

在 `requests.rs` 的 match 中（`"session/rename" => { ... }` 分支之后、`"marketplace/refresh"` 之前）插入：

```rust
"session/rewind-candidates" => {
    let session_id = params
        .get("sessionId")
        .or_else(|| params.get("session_id"))
        .and_then(|v| v.as_str())
        .ok_or_else(|| AcpError::new(-32602, "missing sessionId"))?;
    let history = sessions
        .get(session_id)
        .map(|s| s.history.clone())
        .ok_or_else(|| AcpError::new(-32602, "session not found"))?;
    dispatch::rewind_candidates(&history)
}

"session/rewind-preview" => {
    let session_id = params
        .get("sessionId")
        .or_else(|| params.get("session_id"))
        .and_then(|v| v.as_str())
        .ok_or_else(|| AcpError::new(-32602, "missing sessionId"))?
        .to_string();
    let history = sessions
        .get(&session_id)
        .map(|s| s.history.clone())
        .ok_or_else(|| AcpError::new(-32602, "session not found"))?;
    let event_sink = Arc::new(peri_acp::session::event_sink::TransportEventSink::new(
        transport.clone(), // transport: &Arc<dyn AcpTransport>（签名改动见下方实现注记）
        cfg.session_manager.caps_registry(),
    ));
    dispatch::rewind_preview(params, &history, &event_sink, &session_id).await
}

"session/rewind" => {
    let session_id = params
        .get("sessionId")
        .or_else(|| params.get("session_id"))
        .and_then(|v| v.as_str())
        .ok_or_else(|| AcpError::new(-32602, "missing sessionId"))?
        .to_string();
    let (cwd, history) = {
        let s = sessions
            .get_mut(&session_id)
            .ok_or_else(|| AcpError::new(-32602, "session not found"))?;
        (s.cwd.clone(), s.history.clone())
    };
    let event_sink = Arc::new(peri_acp::session::event_sink::TransportEventSink::new(
        transport.clone(), // transport: &Arc<dyn AcpTransport>（签名改动见下方实现注记）
        cfg.session_manager.caps_registry(),
    ));
    let peri_config_snapshot = Arc::new(cfg.peri_config.read().clone());
    dispatch::rewind_execute(
        params,
        history,
        &cwd,
        &peri_config_snapshot,
        &event_sink,
        None, // auxiliary_model：RewindCommand 不使用
        &peri_agent::agent::AgentCancellationToken::new(),
        Some(cfg.thread_store.clone()),
        Some(session_id.clone()),
        None, // bg_event_tx
        None, // bg_registry
        None, None, None, None, // frozen_*：RewindCommand 不使用
    )
    .await
}
```

> **实现注记**：
> 1. `handle_request` 当前签名（requests.rs:81-87）的 `transport: &dyn AcpTransport` 不是 `Arc`，而 `TransportEventSink::new` 需要 `Arc<dyn AcpTransport>`。将签名改为 `transport: &Arc<dyn AcpTransport>`（调用点仅一处：`acp_server/mod.rs` 的 `run_acp_server` 持有 `transport: Arc<dyn AcpTransport>`，`handle_request(&method, &params, &cfg, &mut sessions, transport.as_ref())` 改为 `&transport`）。分支内 `transport.clone()` 即得 Arc。
> 2. `cfg.thread_store` 的类型确认（`AcpServerConfig` 字段，见 mod.rs:62-88）：`Option<Arc<dyn ThreadStore>>` 或 `Arc<dyn ThreadStore>`——按实际类型传参。
> 3. 执行完成后 history 已截断，但 `SessionState.history` 未更新——与 execute-command 现状一致（Immediate 命令不写回 session state；TUI 靠 RewindCompleted 事件刷新）。保持现状，不做额外写回。

- [x] **Step 5: 跑测试确认通过**

Run: `cargo test -p peri-tui --lib requests`
Expected: PASS（新路由测试 + 现有测试）

- [x] **Step 6: 全量编译**

Run: `cargo check -p peri-tui -p peri-acp`
Expected: PASS

- [x] **Step 7: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/acp_server/requests.rs peri-tui/src/acp_server/requests_test.rs peri-tui/src/acp_server/mod.rs
git commit -m "feat(peri-tui): register session/rewind-candidates, session/rewind-preview, session/rewind RPCs"
```

---

## Task 5: 服务端 — 退役 turn 结束推送（executor_helpers）

**Files:**
- Modify: `peri-acp/src/session/executor_helpers.rs`
- Modify: `peri-acp/src/session/executor.rs`
- Modify: `peri-acp/src/session/executor_test.rs`

删除上一轮修复加入的"每轮 turn 完成推送 rewind-preview"逻辑——候选改为打开面板时查询。

- [x] **Step 1: 删除推送块与 helper**

在 `executor_helpers.rs` 中：
1. 删除 Phase 8.7 推送块（约 540-556 行，含 `let rewind_preview_payload = build_rewind_preview_payload(&messages);` 与 `if let Some(payload) = rewind_preview_payload { event_sink.push_unstable_event(...).await; }`）
2. 删除文件末尾 `build_rewind_preview_payload` 函数（约 645-675 行）
3. 删除模块级 import：`use peri_acp_types::event_data::{RewindMessage, RewindPreview};`

> 注意：`messages` 变量在删除推送后可能变为只被 `AgentState::with_messages` 使用——保持原有的 `let messages: Vec<BaseMessage> = ...` 显式标注即可（`full_compaction_committed()` 元组仍被使用）。

- [x] **Step 2: 移除 executor.rs re-export**

在 `executor.rs` 的 `use executor_helpers::{...}` 列表中删除 `build_rewind_preview_payload`：

```rust
use executor_helpers::{
    build_and_execute_agent_v2, close_channel, collect_result, intercept_immediate_command,
    spawn_event_pump, wait_for_pump, CollectRequest, InterceptRequest, PumpHandle, SpawnPumpRequest,
};
```

- [x] **Step 3: 删除 payload 测试**

在 `executor_test.rs` 中删除三个测试：`test_build_rewind_preview_payload_carries_message_ids_and_roles` / `test_build_rewind_preview_payload_empty_returns_none` / `test_build_rewind_preview_payload_truncates_preview`。

- [x] **Step 4: 验证**

Run: `cargo test -p peri-acp --lib && cargo check -p peri-tui`
Expected: PASS（355 测试中的 payload 测试移除后其余全过；`push_unstable_event` 的 `EventSink` trait 方法保留不动——其他不稳定事件仍在用）

- [x] **Step 5: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-acp/src/session/executor_helpers.rs peri-acp/src/session/executor.rs peri-acp/src/session/executor_test.rs
git commit -m "refactor(peri-acp): retire per-turn rewind-preview push (query-on-open replaces push model)"
```

---

## Task 6: TUI — 新 atoms

**Files:**
- Modify: `peri-tui/src/kit/atoms.rs`

新增 3 个 atom（放在 `REWIND_PREVIEW` 定义之后，约 267 行）：

- [x] **Step 1: 实现**

```rust
/// 回退目标 user 消息文本暂存——候选 Enter 时写入，RewindCompleted 到达后
/// 消费回填输入框；任何失败/取消路径清空。
pub static REWIND_TARGET_TEXT: AtomStatic<Option<String>> = AtomStatic::new(|| None);

/// 文件回退预算状态——候选 Enter 后由 rewind_consumer 写入：
/// `Idle` = 未进入预算阶段（候选视图）；`Executing` = 预算为空自动执行或
/// 用户确认后执行中（弹窗显示"正在回退…"）；`Files(v)` = 待用户确认的预算。
#[derive(Debug, Clone, Default, PartialEq, Eq)]
pub enum RewindBudgetState {
    #[default]
    Idle,
    Executing,
    Files(Vec<RewindFileChange>),
}

/// 单个文件回退预算条目（服务端 `session/rewind-preview` 响应元素）。
#[derive(Debug, Clone, PartialEq, Eq, serde::Deserialize)]
pub struct RewindFileChange {
    pub path: String,
    pub kind: String,
}

pub static REWIND_BUDGET_STATE: AtomStatic<RewindBudgetState> = AtomStatic::new(|| RewindBudgetState::Idle);

/// 候选查询失败信息（Option<String> 错误文案）；None = 查询中或未查询。
pub static REWIND_QUERY_ERROR: AtomStatic<Option<String>> = AtomStatic::new(|| None);
```

- [x] **Step 2: 编译检查**

Run: `cargo check -p peri-tui`
Expected: PASS

- [x] **Step 3: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/kit/atoms.rs
git commit -m "feat(peri-tui): add REWIND_TARGET_TEXT / REWIND_BUDGET_STATE / REWIND_QUERY_ERROR atoms"
```

---

## Task 7: TUI — kit/rewind_candidates.rs（候选查询）

**Files:**
- Create: `peri-tui/src/kit/rewind_candidates.rs`
- Test: `peri-tui/src/kit/rewind_candidates_test.rs`

双击 Esc 时 spawn 异步查询 `session/rewind-candidates`，响应解析后写入 `REWIND_PREVIEW` atom 并触发重渲染。

- [x] **Step 1: 写失败测试**

```rust
//! rewind_candidates 单元测试——响应解析纯函数。

use serde_json::json;

use super::parse_candidates_response;
use crate::kit::atoms::{init_atoms, REWIND_PREVIEW};
use serial_test::serial;

#[test]
fn test_parse_candidates_response_extracts_messages() {
    let resp = json!({
        "messages": [
            { "id": "m1", "preview": "第一轮问题" },
            { "id": "m2", "preview": "第二轮问题" },
        ]
    });

    let candidates = parse_candidates_response(&resp).unwrap();
    assert_eq!(candidates.len(), 2);
    assert_eq!(candidates[0].id, "m1");
    assert_eq!(candidates[0].preview, "第一轮问题");
    assert_eq!(candidates[1].id, "m2");
}

#[test]
fn test_parse_candidates_response_empty_ok() {
    let resp = json!({ "messages": [] });
    let candidates = parse_candidates_response(&resp).unwrap();
    assert!(candidates.is_empty());
}

#[test]
fn test_parse_candidates_response_malformed_returns_err() {
    let resp = json!({ "unexpected": 1 });
    assert!(parse_candidates_response(&resp).is_err());
}

#[test]
#[serial]
fn test_apply_candidates_writes_preview_atom() {
    init_atoms();
    let candidates = vec![
        crate::kit::rewind_candidates::RewindCandidate {
            id: "m1".into(),
            preview: "问题".into(),
        },
    ];
    crate::kit::rewind_candidates::apply_candidates(&candidates);

    let preview = REWIND_PREVIEW.state().read().clone();
    let preview = preview.expect("候选应写入 REWIND_PREVIEW atom");
    assert_eq!(preview.messages.len(), 1);
    assert_eq!(preview.messages[0].id, "m1");
    assert_eq!(preview.messages[0].role, "user");
    assert_eq!(preview.messages[0].preview, "问题");
    assert!(preview.files.is_empty(), "候选查询不携带文件信息");
}
```

- [x] **Step 2: 跑测试确认失败**

Run: `cargo test -p peri-tui --lib rewind_candidates`
Expected: FAIL（模块不存在）

- [x] **Step 3: 实现**

```rust
//! Rewind 候选查询——双击 Esc 时向服务端实时查询 user 消息候选。
//!
//! 替代旧的"每轮 turn 结束推送 preview"模型：候选由打开面板时的
//! `session/rewind-candidates` RPC 一次性获取，写入 `REWIND_PREVIEW` atom，
//! 弹窗组件订阅渲染。

use serde_json::Value;

use crate::kit::atoms::{
    ACP_CLIENT_HANDLE, RENDER_HEARTBEAT, REWIND_PREVIEW, REWIND_QUERY_ERROR,
};
use peri_acp_types::event_data::{RewindMessage, RewindPreview};

/// 单个回退候选。
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct RewindCandidate {
    pub id: String,
    pub preview: String,
}

/// 解析 `session/rewind-candidates` 响应中的消息列表。
pub fn parse_candidates_response(resp: &Value) -> Result<Vec<RewindCandidate>, String> {
    let messages = resp
        .get("messages")
        .and_then(|v| v.as_array())
        .ok_or_else(|| "rewind-candidates 响应缺少 messages 数组".to_string())?;

    messages
        .iter()
        .map(|m| {
            Ok(RewindCandidate {
                id: m
                    .get("id")
                    .and_then(|v| v.as_str())
                    .ok_or_else(|| "候选缺少 id".to_string())?
                    .to_string(),
                preview: m
                    .get("preview")
                    .and_then(|v| v.as_str())
                    .unwrap_or("")
                    .to_string(),
            })
        })
        .collect()
}

/// 将候选写入 REWIND_PREVIEW atom（role 恒 "user"），触发弹窗重渲染。
pub fn apply_candidates(candidates: &[RewindCandidate]) {
    let preview = RewindPreview {
        files: vec![],
        messages: candidates
            .iter()
            .map(|c| RewindMessage {
                id: c.id.clone(),
                role: "user".to_string(),
                preview: c.preview.clone(),
            })
            .collect(),
    };
    *REWIND_PREVIEW.state().write() = Some(preview);
    RENDER_HEARTBEAT.set(RENDER_HEARTBEAT.get().wrapping_add(1));
}

/// 异步发送候选查询（双击 Esc 时 spawn）。
///
/// 成功：`apply_candidates` + 清 REWIND_QUERY_ERROR；
/// 失败：写 REWIND_QUERY_ERROR 错误文案（弹窗显示）。
pub fn spawn_candidates_query() {
    let Some(client) = ACP_CLIENT_HANDLE.get().cloned() else {
        *REWIND_QUERY_ERROR.state().write() =
            Some("ACP client 未初始化，无法查询回退候选".to_string());
        RENDER_HEARTBEAT.set(RENDER_HEARTBEAT.get().wrapping_add(1));
        return;
    };
    let sid = crate::kit::atoms::ACTIVE_SESSION_ID.state().read().clone();
    if sid.is_empty() {
        *REWIND_QUERY_ERROR.state().write() =
            Some("无活动会话，无法查询回退候选".to_string());
        RENDER_HEARTBEAT.set(RENDER_HEARTBEAT.get().wrapping_add(1));
        return;
    }

    tokio::spawn(async move {
        let resp = client
            .send_raw_request(
                "session/rewind-candidates",
                serde_json::json!({ "sessionId": sid }),
            )
            .await;
        match resp {
            Ok(value) => match parse_candidates_response(&value) {
                Ok(candidates) => {
                    *REWIND_QUERY_ERROR.state().write() = None;
                    apply_candidates(&candidates);
                }
                Err(e) => {
                    *REWIND_QUERY_ERROR.state().write() = Some(e);
                    RENDER_HEARTBEAT.set(RENDER_HEARTBEAT.get().wrapping_add(1));
                }
            },
            Err(e) => {
                *REWIND_QUERY_ERROR.state().write() =
                    Some(format!("候选查询失败: {e}"));
                RENDER_HEARTBEAT.set(RENDER_HEARTBEAT.get().wrapping_add(1));
            }
        }
    });
}

#[cfg(test)]
#[path = "rewind_candidates_test.rs"]
mod tests;
```

> 注：`ACTIVE_SESSION_ID` 为 `AtomStatic<String>`（atoms.rs:341，非 OnceLock）——读取用 `.state().read().clone()`，空字符串视为无会话。Task 8 的 consumer 同此。

- [x] **Step 4: 跑测试确认通过**

Run: `cargo test -p peri-tui --lib rewind_candidates`
Expected: PASS（4 个测试）

- [x] **Step 5: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/kit/rewind_candidates.rs peri-tui/src/kit/rewind_candidates_test.rs
git commit -m "feat(peri-tui): add rewind candidates query on panel open"
```

---

## Task 8: TUI — rewind_action.rs 两段式 consumer

**Files:**
- Modify: `peri-tui/src/kit/rewind_action.rs`
- Modify: `peri-tui/src/kit/rewind_action_test.rs`

`RewindAction` 改为两段式：`Preview`（候选 Enter → 暂存目标文本 + 预算查询）与 `Confirm`（预算确认 → 执行）。

- [x] **Step 1: 写失败测试（rewind_action_test.rs 更新/追加）**

```rust
/// 两段式流程：Preview → 预算空 → 自动执行。
/// 验证：REWIND_TARGET_TEXT 被暂存；自动执行路径发送 session/rewind。
///
/// 注：consumer 为 tokio::spawn 异步任务，测试通过 MockTransport 拦截
/// send_raw_request 验证请求方法序列。若现有测试基建无法 mock transport，
/// 降级为验证纯函数：`build_preview_params` / `build_execute_params` 的 JSON 结构。
#[test]
fn test_build_preview_params_carries_target() {
    use super::build_preview_params;
    let params = build_preview_params("test-sid", "m1");
    assert_eq!(params["sessionId"], "test-sid");
    assert_eq!(params["target_message_id"], "m1");
}

#[test]
fn test_build_execute_params_carries_target() {
    use super::build_execute_params;
    let params = build_execute_params("test-sid", "m1");
    assert_eq!(params["sessionId"], "test-sid");
    assert_eq!(params["target_message_id"], "m1");
}
```

> 注：两个 builder 的签名固定为 `(sid: &str, target_message_id: &str)`，sessionId 由 consumer 从 `ACTIVE_SESSION_ID`（AtomStatic，`.state().read().clone()`）读取后显式传入——测试不依赖全局 atom 状态。

- [x] **Step 2: 跑测试确认失败**

Run: `cargo test -p peri-tui --lib rewind_action`
Expected: FAIL（函数不存在）

- [x] **Step 3: 实现**

```rust
//! Rewind 指令消费者——REWIND_ACTION_TX channel → acp_client RPC。
//!
//! 两段式流程（Rewind v2）：
//! 1. `Preview { target_message_id, target_text }`（候选列表 Enter）：
//!    暂存目标文本到 REWIND_TARGET_TEXT → 查询 `session/rewind-preview` 预算
//!    → 预算空：立即执行 `session/rewind`（写 REWIND_BUDGET_STATE=Executing）
//!    → 预算非空：写 REWIND_BUDGET_STATE=Files(预算)，弹窗切预算视图
//! 2. `Confirm { target_message_id }`（预算确认 Enter）：
//!    执行 `session/rewind`（REWIND_BUDGET_STATE=Executing）
//! 执行完成由 RewindCompleted 事件驱动（handle_rewind_completed），
//! 失败路径清理 REWIND_TARGET_TEXT / REWIND_BUDGET_STATE。

use serde_json::{Value, json};
use tokio::sync::mpsc;
use tokio_util::sync::CancellationToken;
use tracing::{error, info, warn};

use crate::acp_client::AcpTuiClient;
use crate::kit::atoms::{
    ACTIVE_SESSION_ID, REWIND_BUDGET_STATE, REWIND_TARGET_TEXT, RewindBudgetState, RewindFileChange,
};

/// Rewind 用户操作——由 RewindPopup 通过 REWIND_ACTION_TX 发送。
#[derive(Debug, Clone)]
pub enum RewindAction {
    /// 候选列表 Enter：触发预算查询。
    Preview {
        target_message_id: String,
        target_text: String,
    },
    /// 预算确认 Enter：执行回退（恒 revert_files=true）。
    Confirm {
        target_message_id: String,
    },
}

/// 构造预算查询参数。
pub fn build_preview_params(sid: &str, target_message_id: &str) -> Value {
    json!({
        "sessionId": sid,
        "target_message_id": target_message_id,
    })
}

/// 构造执行参数。
pub fn build_execute_params(sid: &str, target_message_id: &str) -> Value {
    json!({
        "sessionId": sid,
        "target_message_id": target_message_id,
    })
}

/// 解析预算响应为文件改动列表。
pub fn parse_budget_response(resp: &Value) -> Result<Vec<RewindFileChange>, String> {
    let changes = resp
        .get("file_changes")
        .and_then(|v| v.as_array())
        .ok_or_else(|| "rewind-preview 响应缺少 file_changes 数组".to_string())?;
    changes
        .iter()
        .map(|c| {
            Ok(RewindFileChange {
                path: c
                    .get("path")
                    .and_then(|v| v.as_str())
                    .ok_or_else(|| "预算项缺少 path".to_string())?
                    .to_string(),
                kind: c
                    .get("kind")
                    .and_then(|v| v.as_str())
                    .unwrap_or("")
                    .to_string(),
            })
        })
        .collect()
}

/// 启动 rewind 指令消费者后台任务。
pub fn spawn_rewind_consumer(
    acp_client: AcpTuiClient,
    mut rx: mpsc::UnboundedReceiver<RewindAction>,
    shutdown: CancellationToken,
) -> tokio::task::JoinHandle<()> {
    tokio::spawn(async move {
        info!("kit rewind_consumer: started");
        loop {
            tokio::select! {
                _ = shutdown.cancelled() => {
                    info!("kit rewind_consumer: shutdown signal received, exiting");
                    break;
                }
                msg = rx.recv() => {
                    match msg {
                        None => {
                            info!("kit rewind_consumer: REWIND_ACTION_TX dropped, exiting");
                            break;
                        }
                        Some(action) => {
                            if let Err(e) = handle_action(&acp_client, action).await {
                                error!(error = %e, "kit rewind_consumer: rewind RPC failed");
                                // 失败清理：目标文本与预算状态不留残
                                *REWIND_TARGET_TEXT.state().write() = None;
                                *REWIND_BUDGET_STATE.state().write() = RewindBudgetState::Idle;
                                crate::kit::atoms::RENDER_HEARTBEAT.set(
                                    crate::kit::atoms::RENDER_HEARTBEAT.get().wrapping_add(1),
                                );
                            }
                        }
                    }
                }
            }
        }
    })
}

async fn handle_action(
    acp_client: &AcpTuiClient,
    action: RewindAction,
) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
    if !acp_client.has_session() {
        warn!("kit rewind_consumer: no active session, skipping rewind");
        return Ok(());
    }
    let sid = ACTIVE_SESSION_ID.state().read().clone();

    match action {
        RewindAction::Preview {
            target_message_id,
            target_text,
        } => {
            *REWIND_TARGET_TEXT.state().write() = Some(target_text);
            info!(
                target_message_id = %target_message_id,
                "kit rewind_consumer: querying rewind budget"
            );
            let resp = acp_client
                .send_raw_request(
                    "session/rewind-preview",
                    build_preview_params(&sid, &target_message_id),
                )
                .await?;
            let changes = parse_budget_response(&resp)?;
            if changes.is_empty() {
                // 无文件改动 → 直接执行
                *REWIND_BUDGET_STATE.state().write() = RewindBudgetState::Executing;
                crate::kit::atoms::RENDER_HEARTBEAT
                    .set(crate::kit::atoms::RENDER_HEARTBEAT.get().wrapping_add(1));
                execute_rewind(acp_client, &sid, &target_message_id).await?;
            } else {
                *REWIND_BUDGET_STATE.state().write() = RewindBudgetState::Files(changes);
                crate::kit::atoms::RENDER_HEARTBEAT
                    .set(crate::kit::atoms::RENDER_HEARTBEAT.get().wrapping_add(1));
            }
        }
        RewindAction::Confirm { target_message_id } => {
            *REWIND_BUDGET_STATE.state().write() = RewindBudgetState::Executing;
            crate::kit::atoms::RENDER_HEARTBEAT
                .set(crate::kit::atoms::RENDER_HEARTBEAT.get().wrapping_add(1));
            execute_rewind(acp_client, &sid, &target_message_id).await?;
        }
    }
    Ok(())
}

async fn execute_rewind(
    acp_client: &AcpTuiClient,
    sid: &str,
    target_message_id: &str,
) -> Result<(), Box<dyn std::error::Error + Send + Sync>> {
    info!(target_message_id = %target_message_id, "kit rewind_consumer: executing /rewind");
    acp_client
        .send_raw_request("session/rewind", build_execute_params(sid, target_message_id))
        .await
        .map_err(|e| {
            warn!(error = %e, "kit rewind_consumer: /rewind RPC failed");
            Box::new(e) as Box<dyn std::error::Error + Send + Sync>
        })?;
    Ok(())
}

#[cfg(test)]
#[path = "rewind_action_test.rs"]
mod tests;
```

- [x] **Step 4: 更新旧测试**

`rewind_action_test.rs` 中引用 `RewindAction::Confirm { target_message_id, revert_files }` 的旧测试需改为新变体（去掉 `revert_files` 字段；`Confirm` 不再含 `target_text`，文本暂存由 consumer 在 Preview 阶段完成）。

- [x] **Step 5: 跑测试确认通过**

Run: `cargo test -p peri-tui --lib rewind_action`
Expected: PASS

- [x] **Step 6: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/kit/rewind_action.rs peri-tui/src/kit/rewind_action_test.rs
git commit -m "feat(peri-tui): two-stage rewind consumer (preview budget -> execute)"
```

---

## Task 9: TUI — rewind_popup.rs 重写（候选列表 + 预算视图 + 执行中态）

**Files:**
- Modify: `peri-tui/src/kit/popups/rewind_popup.rs`
- Modify: `peri-tui/src/kit/popups/rewind_popup_test.rs`

重写弹窗：候选列表（只 user 消息，截断显示）+ 预算确认视图（文件列表 + Enter 确认 / Esc 返回）+ 执行中态。删除 files 双视图（RewindView::Files、Tab 切换、鼠标文件区命中）。

- [x] **Step 1: 写失败测试（rewind_popup_test.rs 更新）**

现有测试（7 个）引用 `preview.messages` 含 assistant 条目、`RewindView` 双视图等，需整体更新。新测试覆盖：

```rust
/// 候选视图：2 条 user 候选 + 选中标记（渲染行构造纯函数 build_popup_lines）。
#[test]
#[serial]
fn test_popup_lines_candidates_view() {
    crate::kit::atoms::init_atoms();
    let theme = peri_theme::atoms::THEME_ATOM.state().read().clone();
    let preview = Some(peri_acp_types::event_data::RewindPreview {
        files: vec![],
        messages: vec![
            peri_acp_types::event_data::RewindMessage {
                id: "m1".into(),
                role: "user".into(),
                preview: "第一轮问题".into(),
            },
            peri_acp_types::event_data::RewindMessage {
                id: "m2".into(),
                role: "user".into(),
                preview: "第二轮问题".into(),
            },
        ],
    });
    let lines = build_popup_lines(
        &preview,
        &RewindBudgetState::Idle,
        &None,
        0,
        0,
        &theme,
    );
    let text: String = lines.iter().map(|l| l.to_string()).collect::<Vec<_>>().join("\n");
    assert!(text.contains("回退到（2）"), "候选视图显示数量");
    assert!(text.contains("第一轮问题") && text.contains("第二轮问题"), "渲染全部候选");
    assert!(text.contains(">"), "选中行有标记");
}

/// 候选视图：空候选显示"无可回退"。
#[test]
#[serial]
fn test_popup_lines_empty_candidates() {
    crate::kit::atoms::init_atoms();
    let theme = peri_theme::atoms::THEME_ATOM.state().read().clone();
    let lines = build_popup_lines(
        &Some(peri_acp_types::event_data::RewindPreview {
            files: vec![],
            messages: vec![],
        }),
        &RewindBudgetState::Idle,
        &None,
        0,
        0,
        &theme,
    );
    let text: String = lines.iter().map(|l| l.to_string()).collect::<Vec<_>>().join("\n");
    assert!(text.contains("无可回退"), "空候选提示");
}

/// 查询失败：显示错误文案。
#[test]
#[serial]
fn test_popup_lines_query_error() {
    crate::kit::atoms::init_atoms();
    let theme = peri_theme::atoms::THEME_ATOM.state().read().clone();
    let lines = build_popup_lines(
        &None,
        &RewindBudgetState::Idle,
        &Some("RPC timeout".to_string()),
        0,
        0,
        &theme,
    );
    let text: String = lines.iter().map(|l| l.to_string()).collect::<Vec<_>>().join("\n");
    assert!(text.contains("查询失败: RPC timeout"), "错误文案透出");
}

/// 候选未返回（preview=None 且无错误）：显示加载中。
#[test]
#[serial]
fn test_popup_lines_loading() {
    crate::kit::atoms::init_atoms();
    let theme = peri_theme::atoms::THEME_ATOM.state().read().clone();
    let lines = build_popup_lines(&None, &RewindBudgetState::Idle, &None, 0, 0, &theme);
    let text: String = lines.iter().map(|l| l.to_string()).collect::<Vec<_>>().join("\n");
    assert!(text.contains("加载中"), "加载中提示");
}

/// 预算视图：文件列表 + 确认提示。
#[test]
#[serial]
fn test_popup_lines_budget_view() {
    crate::kit::atoms::init_atoms();
    let theme = peri_theme::atoms::THEME_ATOM.state().read().clone();
    let budget = RewindBudgetState::Files(vec![
        crate::kit::atoms::RewindFileChange {
            path: "src/main.rs".into(),
            kind: "edit".into(),
        },
        crate::kit::atoms::RewindFileChange {
            path: "new_file.txt".into(),
            kind: "write".into(),
        },
    ]);
    let lines = build_popup_lines(&None, &budget, &None, 0, 0, &theme);
    let text: String = lines.iter().map(|l| l.to_string()).collect::<Vec<_>>().join("\n");
    assert!(text.contains("2 个文件改动"), "预算数量");
    assert!(text.contains("[edit] src/main.rs") && text.contains("[write] new_file.txt"), "文件列表");
    assert!(text.contains("Enter 确认回退"), "确认提示");
}

/// 执行中：显示"正在回退"。
#[test]
#[serial]
fn test_popup_lines_executing() {
    crate::kit::atoms::init_atoms();
    let theme = peri_theme::atoms::THEME_ATOM.state().read().clone();
    let lines = build_popup_lines(&None, &RewindBudgetState::Executing, &None, 0, 0, &theme);
    let text: String = lines.iter().map(|l| l.to_string()).collect::<Vec<_>>().join("\n");
    assert!(text.contains("正在回退"), "执行中提示");
}
```

> 注：`build_popup_lines` 为 `RewindPopup` 内部私有函数（见 Step 3/4）——测试经 `super::build_popup_lines` 访问。`Line::to_string()` 使用 ratatui `Line` 的 Display 实现（输出为拼接后的纯文本）。主题读取路径与原组件一致：`use peri_theme::atoms::THEME_ATOM;`（`atoms.rs` 无 re-export）。`RewindPreview`/`RewindMessage` 来自 `peri_acp_types::event_data`（原 `system.rs` 同源）。

- [x] **Step 2: 跑测试确认失败**

Run: `cargo test -p peri-tui --lib rewind_popup`
Expected: FAIL（旧测试引用已删除的 RewindView::Files / 新渲染结构不匹配）

- [x] **Step 3: 重写组件**

核心结构（替换原组件主体，保留 `AreaTracker` / 鼠标命中 / 主题读取等现有骨架）：

```rust
//! 弹窗三态（内部 state）：
//! - Candidates：候选列表（REWIND_PREVIEW.messages，只 user）——Up/Down 选择、Enter 发 Preview
//! - Budget：文件回退预算（REWIND_BUDGET_STATE=Files(v)）——Enter 发 Confirm、Esc 回 Candidates
//! - Executing：正在回退（REWIND_BUDGET_STATE=Executing）——等待 RewindCompleted 关闭
//! 渲染数据源：REWIND_PREVIEW（候选）、REWIND_BUDGET_STATE（预算/执行中）、REWIND_QUERY_ERROR（查询失败）。

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum RewindView {
    Candidates,
    Budget,
    Executing,
}

#[component]
pub fn RewindPopup(mut hooks: Hooks) -> impl Into<AnyElement<'static>> {
    let theme_def = hooks.use_atom(&THEME_ATOM);
    let preview_store = hooks.use_atom(&crate::kit::atoms::REWIND_PREVIEW);
    let preview = preview_store.read().clone();
    let budget_store = hooks.use_atom(&crate::kit::atoms::REWIND_BUDGET_STATE);
    let budget_state = budget_store.read().clone();
    let query_error = hooks
        .use_atom(&crate::kit::atoms::REWIND_QUERY_ERROR)
        .read()
        .clone();
    let _ = budget_store;

    hooks.use_atom(&LANG_VERSION);

    // 视图推导：预算 Files 非空 → Budget；Executing → Executing；否则 Candidates
    let view = match &budget_state {
        RewindBudgetState::Files(v) if !v.is_empty() => RewindView::Budget,
        RewindBudgetState::Executing => RewindView::Executing,
        _ => RewindView::Candidates,
    };
    // 候选选中索引（最新一条 = 回退一步）
    let msg_sel = hooks.use_state(|| 0usize);
    // 预算视图选中索引（默认最新变更 = 第一条）
    let file_sel = hooks.use_state(|| 0usize);

    let msg_count = preview.as_ref().map(|p| p.messages.len()).unwrap_or(0);
    let budget_files = match &budget_state {
        RewindBudgetState::Files(v) => v.clone(),
        _ => vec![],
    };
    let file_count = budget_files.len();

    // 渲染行提取为纯函数（测试友好）——见 Step 4
    let lines: Vec<Line<'static>> = build_popup_lines(
        &preview,
        &budget_state,
        &query_error,
        *msg_sel.read(),
        *file_sel.read(),
        &theme_def.read(),
    );

    // 事件处理：仅 Candidates 视图处理 Up/Down/Enter；Budget 视图处理 Enter/Esc
    hooks.use_event_handler_with_options(
        EventScope::Current,
        EventPriority::Normal,
        EventOptions { hit_test: true },
        move |event| {
            if let Event::Key(key) = event
                && key.kind == KeyEventKind::Press
            {
                match classify_list_nav(&key) {
                    Some(ListNavAction::MoveUp) if view == RewindView::Candidates => {
                        let mut s = msg_sel.write();
                        *s = previous_selection(*s);
                        return EventResult::Consumed;
                    }
                    Some(ListNavAction::MoveDown) if view == RewindView::Candidates => {
                        let mut s = msg_sel.write();
                        *s = next_selection(*s, msg_count);
                        return EventResult::Consumed;
                    }
                    Some(ListNavAction::Confirm) => {
                        match view {
                            RewindView::Candidates => {
                                let target = preview.as_ref().and_then(|p| p.messages.get(*msg_sel.read()));
                                if let Some(m) = target
                                    && let Some(tx) = REWIND_ACTION_TX.get()
                                {
                                    let _ = tx.send(RewindAction::Preview {
                                        target_message_id: m.id.clone(),
                                        target_text: m.preview.clone(),
                                    });
                                }
                                // 弹窗保持打开：等待预算/执行结果
                            }
                            RewindView::Budget => {
                                if let Some(m) = preview.as_ref().and_then(|p| p.messages.get(*msg_sel.read()))
                                    && let Some(tx) = REWIND_ACTION_TX.get()
                                {
                                    let _ = tx.send(RewindAction::Confirm {
                                        target_message_id: m.id.clone(),
                                    });
                                }
                                // 保持打开：等待执行完成
                            }
                            RewindView::Executing => {}
                        }
                        return EventResult::Consumed;
                    }
                    Some(ListNavAction::Cancel) => {
                        match view {
                            RewindView::Budget | RewindView::Executing => {
                                // 预算/执行中态：Esc 回候选并清理（执行中态 Esc 视为取消等待，
                                // RewindCompleted 仍会到达刷新消息区）
                                *REWIND_BUDGET_STATE.state().write() = RewindBudgetState::Idle;
                                *REWIND_TARGET_TEXT.state().write() = None;
                                RENDER_HEARTBEAT.set(RENDER_HEARTBEAT.get().wrapping_add(1));
                                return EventResult::Consumed;
                            }
                            RewindView::Candidates => {
                                close_popup();
                                *REWIND_TARGET_TEXT.state().write() = None;
                                *REWIND_BUDGET_STATE.state().write() = RewindBudgetState::Idle;
                                *REWIND_QUERY_ERROR.state().write() = None;
                                return EventResult::Consumed;
                            }
                        }
                    }
                    _ => {}
                }
            }
            EventResult::Ignored
        },
    );

    // 渲染（保留原 popup 边框/主题骨架）：build_popup_lines 的输出作为内容行
    // ...（原有渲染骨架，lines 替换为 build_popup_lines 返回值）
}
```

- [x] **Step 4: 渲染行纯函数**

```rust
/// 构造弹窗内容行（纯函数，测试友好）。
fn build_popup_lines(
    preview: &Option<RewindPreview>,
    budget_state: &RewindBudgetState,
    query_error: &Option<String>,
    msg_sel: usize,
    file_sel: usize,
    theme: &ThemeSnapshot, // 以实际主题类型为准
) -> Vec<Line<'static>> {
    let mut lines: Vec<Line<'static>> = Vec::new();
    lines.push(Line::from(""));
    let semantic = &theme.semantic;

    match budget_state {
        RewindBudgetState::Executing => {
            lines.push(Line::from("  正在回退…").fg(semantic.text.primary));
            lines.push(Line::from(""));
            lines.push(Line::from(i18n::tr("common-esc-close")).fg(semantic.text.dim));
            return lines;
        }
        RewindBudgetState::Files(files) => {
            lines.push(
                Line::from(format!("  回退将撤销 {} 个文件改动：", files.len()))
                    .fg(semantic.text.primary)
                    .bold(),
            );
            for (i, fc) in files.iter().enumerate().take(8) {
                let is_selected = i == file_sel;
                let prefix = if is_selected { "  > " } else { "    " };
                let kind_label = if fc.kind == "write" { "write" } else { "edit" };
                lines.push(
                    Line::from(format!("{prefix}[{kind_label}] {}", truncate_str(&fc.path, 40)))
                        .fg(if is_selected {
                            theme.component.popup.selected_fg
                        } else {
                            semantic.text.primary
                        }),
                );
            }
            lines.push(Line::from(""));
            lines.push(
                Line::from("  Enter 确认回退 · Esc 返回候选")
                    .fg(semantic.text.dim),
            );
            return lines;
        }
        RewindBudgetState::Idle => {}
    }

    // ── Candidates 视图 ──
    if let Some(err) = query_error {
        lines.push(
            Line::from(format!("  查询失败: {err}")).fg(semantic.status.error),
        );
        lines.push(Line::from(""));
        lines.push(Line::from(i18n::tr("common-esc-close")).fg(semantic.text.dim));
        return lines;
    }
    let Some(p) = preview else {
        lines.push(Line::from("  正在加载回退候选…").fg(semantic.text.muted).italic());
        lines.push(Line::from(""));
        lines.push(Line::from(i18n::tr("common-esc-close")).fg(semantic.text.dim));
        return lines;
    };
    if p.messages.is_empty() {
        lines.push(Line::from("  无可回退的消息。").fg(semantic.text.dim));
        lines.push(Line::from("  完成一轮对话后双击 Esc 即可回滚。").fg(semantic.text.dim));
        lines.push(Line::from(""));
        lines.push(Line::from(i18n::tr("common-esc-close")).fg(semantic.text.dim));
        return lines;
    }
    lines.push(
        Line::from(format!("  回退到（{}）", p.messages.len()))
            .fg(semantic.text.primary)
            .bold(),
    );
    for (i, msg) in p.messages.iter().enumerate().take(8) {
        let is_selected = i == msg_sel;
        let prefix = if is_selected { "  > " } else { "    " };
        lines.push(
            Line::from(format!("{prefix}{}", truncate_str(&msg.preview, 40)))
                .fg(if is_selected {
                    theme.component.popup.selected_fg
                } else {
                    semantic.text.primary
                }),
        );
    }
    if p.messages.len() > 8 {
        lines.push(
            Line::from(format!("    ... and {} more", p.messages.len() - 8))
                .fg(semantic.text.dim),
        );
    }
    lines.push(Line::from(""));
    lines.push(Line::from("  Enter 回退 · Esc 关闭").fg(semantic.text.dim));
    lines
}
```

> 注：`ThemeSnapshot` 类型名与 `theme.component.popup.selected_fg` 字段以原组件引用为准（原文件 `let popup_tokens = &theme_def.read().component.popup;`），实现时对齐。

- [x] **Step 5: 更新/新增测试**

`rewind_popup_test.rs` 更新为对 `build_popup_lines` 纯函数测试（若组件级测试可行则保留组件级）：
- 候选视图：2 条 user 候选渲染、空候选显示"无可回退"、查询错误显示错误文案、preview None 显示"加载中"
- 预算视图：Files 非空显示文件列表 + 确认提示
- 执行中：Executing 显示"正在回退…"

- [x] **Step 6: 跑测试确认通过**

Run: `cargo test -p peri-tui --lib rewind_popup`
Expected: PASS

- [x] **Step 7: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/kit/popups/rewind_popup.rs peri-tui/src/kit/popups/rewind_popup_test.rs
git commit -m "feat(peri-tui): rewrite rewind popup with candidates list, budget confirm view, executing state"
```

---

## Task 10: TUI — event_handlers.rs 双击 Esc 触发查询

**Files:**
- Modify: `peri-tui/src/kit/event_handlers.rs:218-222`

- [x] **Step 1: 修改**

```rust
if is_double_esc {
    // Rewind v2：打开面板 + 实时查询候选（查询响应写 REWIND_PREVIEW atom，
    // 弹窗订阅渲染；查询失败写 REWIND_QUERY_ERROR）。
    crate::kit::rewind_candidates::spawn_candidates_query();
    open_popup(PopupKind::Rewind);
    return EventResult::Consumed;
}
```

并清理：原 `let _ = REWIND_PREVIEW.state();` 行（无实际作用）与不再使用的 `REWIND_PREVIEW` import（若仅此处使用则从 use 列表移除）。

- [x] **Step 2: 验证**

Run: `cargo check -p peri-tui`
Expected: PASS（`event_handlers_test.rs` 若无双击 Esc 相关断言则不受影响；若有，按新行为更新——双击 Esc 后 `spawn_candidates_query` 仅当 `ACP_CLIENT_HANDLE` 未初始化时写错误 atom，测试可断言该路径）

- [x] **Step 3: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/kit/event_handlers.rs
git commit -m "feat(peri-tui): double-esc opens rewind popup and queries candidates"
```

---

## Task 11: TUI — system.rs（回填输入框 + preview handler 退役）

**Files:**
- Modify: `peri-tui/src/kit/acp_events/system.rs`
- Modify: `peri-tui/src/kit/acp_events.rs`（dispatch match 中移除 `RewindPreview` arm）
- Modify: `peri-tui/src/kit/acp_events_test.rs`

- [x] **Step 1: 删除 `handle_rewind_preview`**

删除 `system.rs` 中 `handle_rewind_preview` 函数（99-106 行）及 `dispatch_and_notify` 中对应的 `AcpEventData::RewindPreview(rp) => handle_rewind_preview(state, rp)` arm（在 `acp_events.rs`）。`RewindPreview` / `RewindMessage` 的 import 若不再使用则移除（`handle_rewind_completed` 中重建候选仍用 `RewindPreview`/`RewindMessage`，保留）。

- [x] **Step 2: 写失败测试（acp_events_test.rs 追加）**

```rust
/// RewindCompleted 后：目标文本回填输入框（INPUT_RESTORE_TEXT）并触发心跳。
#[test]
#[serial]
fn test_rewind_completed_restores_target_text_to_input() {
    crate::kit::atoms::init_atoms();
    *VIEW_MODELS.state().write() = ViewModelsSnapshot::default();
    // 预置 REWIND_TARGET_TEXT（候选 Enter 时暂存）
    *crate::kit::atoms::REWIND_TARGET_TEXT.state().write() =
        Some("需要重新编辑的问题".to_string());
    let hb_before = *crate::kit::atoms::RENDER_HEARTBEAT.state().read();

    let mut state = make_bridge_state(); // 复用现有测试的 BridgeState 构造
    let messages_json = serde_json::json!([
        {"role": "user", "id": "msg-1", "content": "历史用户消息"},
    ])
    .to_string();
    dispatch_and_notify(&mut state, &AcpEventData::RewindCompleted { messages_json });

    // 回填通道被写入
    let restore = crate::kit::atoms::INPUT_RESTORE_TEXT
        .get()
        .and_then(|mu| mu.lock().clone());
    assert_eq!(restore.as_deref(), Some("需要重新编辑的问题"));
    // 心跳递增触发 InputArea use_effect
    assert!(
        *crate::kit::atoms::RENDER_HEARTBEAT.state().read() > hb_before,
        "回填必须触发 RENDER_HEARTBEAT"
    );
    // 消费后清空暂存
    assert!(
        crate::kit::atoms::REWIND_TARGET_TEXT.state().read().is_none(),
        "REWIND_TARGET_TEXT 消费后应清空"
    );
}

/// RewindCompleted 无目标文本（如直接执行路径异常）时不写回填。
#[test]
#[serial]
fn test_rewind_completed_without_target_text_no_restore() {
    crate::kit::atoms::init_atoms();
    *VIEW_MODELS.state().write() = ViewModelsSnapshot::default();
    *crate::kit::atoms::REWIND_TARGET_TEXT.state().write() = None;

    let mut state = make_bridge_state();
    let messages_json = serde_json::json!([]).to_string();
    dispatch_and_notify(&mut state, &AcpEventData::RewindCompleted { messages_json });

    let restore = crate::kit::atoms::INPUT_RESTORE_TEXT
        .get()
        .and_then(|mu| mu.lock().clone());
    assert!(restore.is_none(), "无目标文本不应触发回填");
}
```

> 注：`INPUT_RESTORE_TEXT` 为 `OnceLock<Mutex<Option<String>>>`——测试读取用 `.get().and_then(|mu| mu.lock().clone())`；注意测试间残留（`take` 语义在 InputArea effect 中），若断言受污染，测试开头先 `take` 清空。

- [x] **Step 3: 跑测试确认失败**

Run: `cargo test -p peri-tui --lib rewind_completed`
Expected: FAIL（回填逻辑未实现）

- [x] **Step 4: 实现回填**

在 `handle_rewind_completed` 末尾（`push_acp_state(state)` 之前）加入：

```rust
// Rewind v2：回填目标 user 消息文本到输入框（复用 TurnInterrupted 的回填通道）。
// 消费 REWIND_TARGET_TEXT → INPUT_RESTORE_TEXT + 心跳 → InputArea use_effect
// 写入编辑态并替换草稿；焦点随 close_popup（RewindCompleted 到达前弹窗已由
// consumer 流程关闭或停留在执行中态，此处统一关闭）。
if let Some(target_text) = crate::kit::atoms::REWIND_TARGET_TEXT.state().write().take() {
    let mu = crate::kit::atoms::INPUT_RESTORE_TEXT
        .get_or_init(|| parking_lot::Mutex::new(None));
    *mu.lock() = Some(target_text);
    crate::kit::atoms::RENDER_HEARTBEAT
        .set(crate::kit::atoms::RENDER_HEARTBEAT.get().wrapping_add(1));
}
// 回退完成：预算状态复位、查询错误清空；弹窗关闭（执行完成）
*REWIND_BUDGET_STATE.state().write() = RewindBudgetState::Idle;
*REWIND_QUERY_ERROR.state().write() = None;
crate::kit::popup_overlay::close_popup();
```

- [x] **Step 5: 更新旧测试**

`acp_events_test.rs` 中 `test_rewind_preview_does_not_open_popup`（539-581 行）删除（handler 已退役）；`test_rewind_completed_replaces_committed` 的 REWIND_PREVIEW 重建断言保留（重建逻辑不变），并确认其不受回填新逻辑影响（REWIND_TARGET_TEXT 为 None → 不回填）。

- [x] **Step 6: 跑测试确认通过**

Run: `cargo test -p peri-tui --lib rewind_completed && cargo test -p peri-tui --lib acp_events`
Expected: PASS

- [x] **Step 7: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/kit/acp_events/system.rs peri-tui/src/kit/acp_events.rs peri-tui/src/kit/acp_events_test.rs
git commit -m "feat(peri-tui): restore target text to input on rewind complete; retire rewind-preview handler"
```

---

## Task 12: TUI — 会话边界清理（/clear、thread 切换、close_popup）

**Files:**
- Modify: `peri-tui/src/kit/popup_overlay.rs`
- Modify: `peri-tui/src/kit/submit_consumer.rs`
- Modify: `peri-tui/src/kit/thread_load_consumer.rs`
- Modify: `peri-tui/src/kit/popup_overlay_test.rs`

- [x] **Step 1: close_popup(Rewind) 清理预算/目标/错误，保留候选**

在 `popup_overlay.rs` 的 `close_popup` 中，`PopupKind::Rewind` 分支（现状为空，112 行）：

```rust
// Rewind：REWIND_PREVIEW 保留（候选跟随会话生命周期，关闭后可再开），
// 但预算状态 / 目标文本 / 查询错误随弹窗关闭清空（下次打开重新查询）。
PopupKind::Rewind => {
    *atoms::REWIND_BUDGET_STATE.state().write() = atoms::RewindBudgetState::Idle;
    *atoms::REWIND_TARGET_TEXT.state().write() = None;
    *atoms::REWIND_QUERY_ERROR.state().write() = None;
}
```

- [x] **Step 2: /clear 清理**

在 `submit_consumer.rs` 的 `handle_clear_submit` 中（现有 `*crate::kit::atoms::REWIND_PREVIEW.state().write() = None;` 之后）追加：

```rust
*atoms::REWIND_TARGET_TEXT.state().write() = None;
*atoms::REWIND_BUDGET_STATE.state().write() = atoms::RewindBudgetState::Idle;
*atoms::REWIND_QUERY_ERROR.state().write() = None;
```

- [x] **Step 3: thread 切换清理**

在 `thread_load_consumer.rs` 的 `handle_load` 中（现有 `*crate::kit::atoms::REWIND_PREVIEW.state().write() = None;` 之后）追加同样三行（import 同步更新）。

- [x] **Step 4: 更新 popup_overlay_test.rs**

`test_close_popup_clears_payload_atoms` 的 Rewind 断言更新：

```rust
open_popup(PopupKind::Rewind);
// 预置预算/目标/错误（close_popup 应清空）
*atoms::REWIND_TARGET_TEXT.state().write() = Some("t".to_string());
*atoms::REWIND_BUDGET_STATE.state().write() = atoms::RewindBudgetState::Executing;
*atoms::REWIND_QUERY_ERROR.state().write() = Some("e".to_string());
close_popup();
assert!(
    atoms::REWIND_PREVIEW.state().read().is_some(),
    "REWIND_PREVIEW should NOT be cleared after close_popup — 候选跟随会话生命周期"
);
assert!(
    atoms::REWIND_TARGET_TEXT.state().read().is_none()
        && *atoms::REWIND_BUDGET_STATE.state().read() == atoms::RewindBudgetState::Idle
        && atoms::REWIND_QUERY_ERROR.state().read().is_none(),
    "close_popup 应清空预算/目标/查询错误"
);
```

- [x] **Step 5: 验证**

Run: `cargo test -p peri-tui --lib popup_overlay && cargo test -p peri-tui --lib submit_consumer && cargo test -p peri-tui --lib thread_load_consumer`
Expected: PASS

- [x] **Step 6: Commit（仅用户要求时）**（跳过——用户未要求 commit）

```bash
git add peri-tui/src/kit/popup_overlay.rs peri-tui/src/kit/submit_consumer.rs peri-tui/src/kit/thread_load_consumer.rs peri-tui/src/kit/popup_overlay_test.rs
git commit -m "fix(peri-tui): clear rewind budget/target/query-error at session boundaries"
```

---

## Task 13: 全量验证

**Files:** 无

- [x] **Step 1: 全量测试**

Run:
```bash
cargo test -p peri-acp --lib
cargo test -p peri-tui --lib
```
Expected: 全绿（peri-acp 355 减 3 个 payload 测试；peri-tui 新增 rewind 相关测试后全过）

- [x] **Step 2: Clippy**

Run: `cargo clippy --workspace --all-targets -- -D warnings`
Expected: exit 0

- [x] **Step 3: 手动验证清单**

1. 启动 `cargo run -p peri-tui -- -a`
2. 对话数轮（含一次 Write/Edit 工具调用）
3. 双击 Esc → 弹窗显示**加载中 → user 消息候选列表**（无 assistant）
4. 选择含文件改动的目标 → Enter → 弹窗切**预算视图**（文件列表 + 确认提示）
5. Enter → "正在回退…" → 消息区回退到目标之前 + **目标文本回填输入框** + 焦点在输入框
6. 选择无文件改动的目标 → Enter → 直接回退（无预算视图）
7. `/clear` 后双击 Esc → 候选查询返回空 → "无可回退"
8. 回退后再次双击 Esc → 候选为回退后的消息（可连续回退）

- [x] **Step 4: 回归检查（服务端遗留代码）**

Run: `grep -rn "rewind-preview" --include="*.rs" peri-acp/src peri-tui/src`
Expected: 仅剩 `peri-tui/src/kit/acp_events.rs`（`AcpEventData::RewindPreview` 变体定义，若未删除）与文档引用；无推送/发送方残留。若 `AcpEventData::RewindPreview` 变体已无消费者，保留定义（协议类型，向后兼容）或删除（连同 `handle_rewind_preview` 已删）——以 Task 11 执行结果为准，删除则同步清理 `peri-acp-types` 的事件解码分支。

---

## 计划自审记录

**1. Spec 覆盖：**
- D1（删除并回填输入框）→ Task 8（REWIND_TARGET_TEXT 暂存）+ Task 11（RewindCompleted 回填）
- D2（最佳努力复原）→ Task 1（复用 extract_file_changes）+ Task 3（RewindCommand 复用），复原逻辑零改动
- D3（只列 user 消息）→ Task 2（user-only 提取）+ Task 9（候选列表渲染）
- D4（打开面板查询一次）→ Task 7 + Task 10；Task 5 退役推送
- D5（回填自动聚焦）→ Task 11（INPUT_RESTORE_TEXT + close_popup 后焦点回输入框）
- D6（预算先行）→ Task 3（预算 RPC）+ Task 8（两段式 consumer）+ Task 9（预算视图）
- D7（持久化硬删除）→ Task 3（复用 RewindCommand.delete_messages，无软删除）

**2. 占位符扫描：** 已消除（`transport_arc_clone()` 等仅为实现注记说明，非占位；各"以实际为准"处均给了明确的查找指令与降级策略）。

**3. 类型一致性：**
- `RewindAction::Preview{target_message_id, target_text}` / `Confirm{target_message_id}` — Task 8 定义，Task 9 消费，一致
- `RewindBudgetState::{Idle, Executing, Files(Vec<RewindFileChange>)}` — Task 6 定义，Task 8/9/11/12 使用，一致
- `parse_budget_response` / `build_preview_params` / `build_execute_params` — Task 8 定义与测试，一致
- `dispatch::rewind_candidates` / `rewind_preview` / `rewind_execute` — Task 2/3 定义，Task 4 注册调用，一致

**4. 首轮自审修复（2026-08-01，写毕后）：**
- Task 8 Step 1 测试与实现签名错位：`build_execute_params("m1")` 单参数（实现为双参数 `(sid, target_message_id)`）、`build_preview_params("m1", "目标文本")` 断言与参数顺序矛盾 → 统一为 `("test-sid", "m1")` + 匹配断言，并删除"签名改为 3 参数"的矛盾注记
- Task 8 Step 3 `ACTIVE_SESSION_ID` 取值：原写 `.get().cloned().unwrap_or_default()`（OnceLock 语义），实际为 `AtomStatic<String>` → 改 `.state().read().clone()`
- Task 9 Step 1 空壳测试（仅注释无断言）→ 替换为 `build_popup_lines` 纯函数 6 个断言测试（候选/空候选/查询失败/加载中/预算/执行中），与 Step 4 实现逐行对应；`THEME_ATOM` 导入路径修正为 `peri_theme::atoms::THEME_ATOM`（atoms.rs 无 re-export，已实测确认）
- Task 3 `changes.retain(|_| true)` no-op 冗余 → 删除，语义说明并入注释
- Task 4 悬空 `transport_arc_clone()` → 明确为 `transport.clone()`，注记 1 改写为确定的签名改动指令（`handle_request` 改 `&Arc<dyn AcpTransport>`，唯一调用点 `acp_server/mod.rs` 同步）
